# 项目总纲

## 1. 这份文件解决什么问题

本项目曾把“完成一个可运行的小原型”写成“完成任务书中的整项任务”。这样虽然每周都有成果，但会掩盖缺失项，最终可能得到一组能单独演示的模块，却无法通过任务书的整体验收。

从现在起，本文件负责把任务书、当前代码和每周开发工作对齐。它不是进度日志，而是判断“接下来做什么”和“什么时候才算完成”的最高层路线图。

项目文件按以下顺序解释：

1. 任务书定义最终交付范围。
2. `ROADMAP.md` 把任务书拆成正式任务、阶段门槛和周计划。
3. `SPEC.md` 只描述当前正在处理的一个小功能及其边界。
4. `checklist.json` 记录正式任务状态、缺口和验收证据。
5. `progress.txt` 保留按日期发生的工作记录，不用历史记录代替当前状态。

## 2. “完成”的统一口径

周内里程碑完成，不等于任务书正式任务完成。

正式任务只有同时满足以下条件，才能在 `checklist.json` 中标记为 `done`：

- 任务书列出的必需能力全部实现，没有用缩小后的原型替代原要求；
- 核心行为有自动化测试，失败路径也经过验证；
- 有可以复现的运行入口、示例或报告；
- README 或专项文档说明了输入、输出、边界和运行方式；
- `checklist.json` 写明可核查的代码、测试和文档证据；
- 运行 README 中的验收命令成功。

若只完成其中一部分，正式任务保持 `in_progress`，已完成部分写入 `completed_slices`。

## 3. 当前真实基线

截至 2026-08-04，阶段一还没有通过整体验收。阶段二虽然已有金融数据契约和技术分析原型，但不能据此认定 2.1 或 2.2 完成。

| 正式任务 | 当前状态 | 已有成果 | 主要缺口 |
| --- | --- | --- | --- |
| 1.1 Harness 规范落地 | 已完成 | 目录、Git、清单、进度记录和小步提交机制 | 后续继续按本总纲维护 |
| 1.2 Loop Engineering | 进行中 | 有限步循环、历史、完成条件和失败重试 | 计划/行动/观察/反思、工具调用、三层记忆、定时与递归循环、工作区隔离、技能和上下文注入 |
| 1.3 Graph Engineering | 进行中 | DAG、拓扑顺序、条件分支、状态合并和 JSON Checkpoint | YAML/JSON 工作流定义、并行、节点重试、超时、熔断和可视化 |
| 1.4 Harness SDK | 进行中 | 统一 Harness、可插拔 Guardrail 和 trace | JSON Schema、来源过滤、限流、关键词阻断、交叉验证五类正式 Guardrail |
| 2.1 数据基础设施 | 进行中 | 数据契约和人工模拟行情 | AKShare/Tushare 工具适配，行情、财务、宏观行业、新闻公告等真实数据类别 |
| 2.2 四类分析 Agent | 进行中 | 简化版技术分析 Agent | 技术 Agent 的完整闭环，以及基本面、行业、大盘宏观三个 Agent、各自 Harness 配置和样例报告 |
| 3.1 综合研判与辩论 | 未开始 | 无 | 汇总、分歧识别、辩论和偏差控制 |
| 3.2 Trader 与 Risk Manager | 未开始 | 无 | 模拟交易建议、仓位约束、风险校验和人工确认边界 |
| 3.3 完整金融 Graph | 未开始 | 无 | 完整编排、至少 20 只股票、条件流转和断点恢复 |
| 4.1 回测系统 | 未开始 | 无 | 信号/执行时间分离、交易成本、指标和报告 |
| 4.2 工业化 Harness | 未开始 | 无 | 可观测性、评估器、熔断、最小权限和对比实验 |
| 4.3 模拟运行与交付 | 未开始 | 无 | 模拟盘、完整文档和可复现实验；非金融案例为加分项 |

## 4. 阶段门槛

项目按门槛推进，不再因为周数到了就自动进入下一阶段。

### 门槛 A：平台底座可验收

任务 1.1 至 1.4 全部完成。Echo Agent、完整 Loop、Graph Engine 和五类 Guardrail 都能独立运行，并有测试和文档证据。

### 门槛 B：金融分析层可验收

任务 2.1 和 2.2 全部完成。真实数据适配覆盖任务书要求的类别，四类分析 Agent 均通过 Harness 运行，输出结构化、可追溯，并能生成样例报告。

### 门槛 C：端到端决策流可验收

任务 3.1 至 3.3 全部完成。综合研判、辩论、Trader、Risk Manager 和完整 Graph 跑通至少 20 只股票；系统只给出模拟建议，不执行真实交易。

### 门槛 D：工程化交付可验收

任务 4.1 至 4.3 全部完成。回测、可观测性、质量评估、熔断、权限控制、Harness 对比实验和模拟运行都有可复现证据。

## 5. 修正版周计划

下面是以“完整达到任务书目标”为前提的基线排期。它按当前学习节奏和剩余缺口估算，可以根据每周验收结果调整，但不能通过隐去需求来追赶周数。

| 周次 | 主要目标 | 对应正式任务 | 周末验收点 |
| --- | --- | --- | --- |
| 第 5 周 | 补齐五类 Guardrail 和 Harness 配置 | 1.4 | 五类规则均有通过/拒绝测试和统一 trace |
| 第 6 周 | 增加外部工作流定义、节点重试和超时 | 1.3 | YAML/JSON 定义可加载，失败策略可验证 |
| 第 7 周 | 增加并行、熔断和 Graph 可视化 | 1.3 | 分支并行可观测，熔断可复现，生成流程图 |
| 第 8 周 | 补齐计划、行动、观察、反思和工具调用 | 1.2 | Echo 之外的任务能走完整 Agent Loop |
| 第 9 周 | 增加三层记忆、定时/递归循环、工作区隔离与上下文注入 | 1.2 | 通过门槛 A 的综合验收 |
| 第 10 周 | 接入最小真实行情数据路径 | 2.1 | AKShare/Tushare 至少一条真实日线链路可验证且保留时间与来源 |
| 第 11 周 | 补齐财务、宏观行业、新闻公告等数据类别 | 2.1 | 数据工具覆盖任务书类别，外部异常有稳定降级 |
| 第 12 周 | 完成技术与基本面 Agent | 2.2 | 两个 Agent 独立运行，有 Harness 配置和样例报告 |
| 第 13 周 | 完成行业与大盘宏观 Agent | 2.2 | 四个 Agent 齐全，通过门槛 B |
| 第 14 周 | 综合汇总、分歧检测和辩论 | 3.1 | 可展示一致意见、分歧和带来源的结论 |
| 第 15 周 | Trader 与 Risk Manager | 3.2 | 建议经过确定性仓位和风险规则；真实下单关闭 |
| 第 16 周 | 完整金融 Graph 与 20 只股票验证 | 3.3 | 端到端流程、条件分支和恢复通过门槛 C |
| 第 17 周 | 回测系统 | 4.1 | 无未来数据，区分信号和执行时间，计入成本 |
| 第 18 周 | 可观测性、评估、熔断、权限和 Harness 对比实验 | 4.2 | 有指标和实验数据说明 Harness 的实际影响 |
| 第 19 周 | 模拟运行、文档收口和最终复现 | 4.3 | 一条命令可复现主要结果，通过门槛 D |

原任务书的 12 周排期已经不能在当前进度下原样成立。若必须把总周期压回 12 周，只能并行开发、减少教学停顿或明确删减交付范围；默认计划不采用这种做法。

## 6. 当前唯一主线

下一项正式工作是第 5 周：完成任务 1.4 的五类 Guardrail。此前计划的“把技术分析接入最小金融 Graph”暂缓，因为它会继续扩大阶段二原型，却不能补上阶段一的验收缺口。

每次开始新工作时，依次回答四个问题：

1. 它对应任务书中的哪一项正式任务？
2. 它补的是哪个明确缺口？
3. 本次结束后，正式任务是 `done` 还是仍为 `in_progress`？
4. 代码、测试、运行结果和文档证据分别在哪里？

答不清这四个问题，就不开始实现。

## 7. 始终不变的边界

- 真实交易默认关闭，当前只允许模拟撮合和人工确认后的候选建议。
- 外部数据必须保留 `source`、`timestamp` 和 `as_of`。
- 指标、仓位、风控和回测结果由确定性代码计算；LLM 只能解释或提出候选方案。
- 回测严格区分信号时间和执行时间，禁止未来数据。
- API 密钥、真实账户信息和本地 `.env` 不进入仓库。
- 加分项不能挤占任务书必做项的时间。
