# 项目协作约定

## 开始工作前

1. 先阅读 `ROADMAP.md`，确认当前阶段门槛和正式任务缺口。
2. 再阅读 `SPEC.md`，确认本次小功能的目标和明确不做的事情。
3. 检查 `checklist.json` 和 `progress.txt`，不要重复已经完成的工作。
4. 一次只处理一个可验收的小功能。

周内里程碑完成不等于任务书正式任务完成。只有 `ROADMAP.md` 中的验收条件全部满足，才能把正式任务标记为 `done`；否则应保留 `in_progress`，并记录已完成部分和剩余缺口。

## 完成功能后

1. 为行为补充或更新测试。
2. 运行 README 中的验证命令。
3. 更新 `checklist.json` 的状态和证据路径。
4. 在 `progress.txt` 记录日期、变化、验证结果和下一步。

## 项目边界

- 真实交易默认关闭；任何下单能力都必须经过人工确认，当前阶段只允许模拟撮合。
- 外部数据必须保留 `source`、`timestamp` 和数据对应的 `as_of` 时间。
- 指标、仓位、风控和回测结果由确定性代码计算，LLM 只能解释或提出候选方案。
- 回测必须区分信号时间和执行时间，禁止使用未来数据。
- 不把 API 密钥、真实账户信息或本地 `.env` 提交到仓库。
- 修改范围要与当前 checklist 项目一致，不为了“看起来完整”提前实现后续阶段。

## 文档语言

项目说明和进度记录默认使用简体中文；代码中的公共接口、字段和错误信息使用清晰、稳定的英文命名。
