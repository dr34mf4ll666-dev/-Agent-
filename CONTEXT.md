# 项目领域词汇

本文件只记录项目中容易混淆的领域词，不记录具体实现步骤。

| 词汇 | 本项目中的含义 |
| --- | --- |
| 证券标的 | 可以被研究的上市证券，用统一代码、交易所和上市状态识别。当前应用范围是沪深 A 股。 |
| 证券主数据 | 描述证券身份、行业归属、可用数据能力和验证状态的版本化资料；它不是行情数据，也不等同于一次分析报告。 |
| 行业 | 面向客户的稳定分类，例如“银行”“酿酒”“电力”；它与数据源返回的行业板块名称分开保存。 |
| 分析行业标签 | 为了调用某个数据源而使用的板块名称，例如“金融行业”“酿酒行业”“电力行业”；它可能随数据源变化，不能替代证券主数据中的行业。 |
| 验证标的 | 已经通过项目规定的数据、四维研究和安全边界检查的证券。只有验证标的才可以进入客户正式目录。 |
| 数据能力 | 某个标的当前允许使用的能力集合，例如最新行情、财务数据、行业数据或离线复现快照；声明能力不等于伪造一份数据。 |
| 快照能力 | 标的是否存在可重复读取的历史验证快照。它描述可复现性，不代表当前市场状态。 |
| 数据质量状态 | 一次冻结分析快照的可用性结论：`complete` 表示关键数据完整，`degraded` 表示使用了备用来源/缓存或缺少非关键数据，`blocked` 表示关键数据不完整或来源时间契约无效；它不是上涨概率。 |
| 运行指纹 | 由快照、证券主数据、代码、配置、模型治理策略和报告版本组成的运行身份摘要；它用于解释和复现，不包含密钥、Prompt 或完整行情。 |
| 差异原因 | 两份冻结报告比较时，对行情时间、数据源状态、证券主数据、确定性规则和模型解释策略变化的分类说明。 |
