项目进度日志
====================

[2026-07-31] 第 0 周初始化开始
- 已确认项目当前只有任务书文档，没有代码和进度文件。
- 清理了文档阅读产生的临时预览目录，避免污染项目根目录。
- 已建立项目规范、目录职责、功能清单和最小测试入口。
- 当前安全默认值：使用 mock/fixture，不接真实交易。

[2026-07-31] 第 0 周初始化完成
- 骨架测试：5 项全部通过。
- Git：已初始化并创建提交 `6b7769f`。
- 第 0 周交付：规范、目录骨架、配置示例、Python 包入口、离线测试、功能清单和进度日志。

下一步
- 进入阶段一：先实现 Echo Agent，再实现最小 Harness 和 Loop 闭环。

[2026-07-31] 第一周完成
- 定义 `AgentRequest`、`AgentResponse`、`Agent`、`Guardrail` 和 `HarnessResult` 公共契约。
- 实现确定性的 `EchoAgent`，不依赖网络、模型或金融数据。
- 实现 `AgentHarness`：前置检查、Agent 执行、后置检查、可插拔 Guardrail 和有序 trace。
- 失败时保留失败阶段、原始异常和已有 trace；本地文档仍保持不被 Git 跟踪。
- 验证结果：10 项 unittest 全部通过。

下一步
- 第二周实现 Loop 的多步运行、最大步数和失败重试。

[2026-07-31] 第二周完成
- 定义 `LoopState`、`LoopEvent`、`LoopResult` 和 Loop 错误类型。
- 实现 `LoopRunner`：每一步通过 Harness，支持历史上下文、完成条件、最大步数和失败重试。
- 验证正常完成、历史传递、最大步数退出、重试成功、重试耗尽和参数边界。
- 验证结果：15 项 unittest 全部通过。

下一步
- 第三周实现最小 Graph/DAG：节点、条件分支和 Checkpoint 的边界。

[2026-08-03] 第三周完成
- 定义 `GraphState`、`GraphEdge`、`GraphDefinition`、`GraphResult` 和 Graph 错误类型。
- 实现确定性的 DAG 运行器：执行前校验、拓扑顺序、状态合并、条件分支和跳过传播。
- 实现 JSON Checkpoint：节点完成、跳过或失败后保存，恢复时不重复运行已完成节点。
- 验证正常 DAG、环检测、条件分支和失败恢复；本地任务书文档继续保持不被 Git 跟踪。
- 验证结果：19 项 unittest 全部通过，源码和测试编译检查通过。

下一步
- 进入阶段二：先定义带 `source`、`timestamp`、`as_of` 的金融数据契约，再实现第一个离线专业 Agent。

[2026-08-03] Graph 可运行演示完成
- 新增 `Scripts/demo_graph.py`，可以直接从命令行观察节点执行、条件分支、节点状态和共享状态。
- 默认模拟 `recoverable` 节点首次失败，并自动从 JSON Checkpoint 恢复；已完成节点不会重复运行。
- Checkpoint 默认写入已忽略的 `checkpoints/`，不会进入 Git；支持切换分支、关闭故障和自定义存档路径。
- 新增子进程集成测试，验证真实命令、终端输出和最终 Checkpoint；验证结果：20 项 unittest 全部通过，源码、测试和脚本编译检查通过。

下一步
- 进入阶段二的第一个小任务：定义金融数据契约和离线 fixture，暂不接入真实行情 API。

[2026-08-04] 阶段二金融数据契约完成
- 新增 `MarketBar`：统一解析 OHLCV、证券代码、`source`、`timestamp` 和 `as_of`，价格使用 `Decimal`。
- 新增 `MarketDataSeries`：要求序列非空、只包含一个证券代码，并按 `as_of` 严格递增。
- 确定性校验覆盖价格范围、非正价格、负数或非整数成交量、缺失字段、无时区时间、未来 `as_of` 和错误外部格式。
- 新增 5 根明确标注为 `synthetic_fixture` 的人工日线数据，不代表真实证券或投资结果。
- 验证结果：新增 10 项金融契约测试，项目共 30 项 unittest 全部通过；源码、测试和脚本编译检查通过。

下一步
- 使用当前离线数据实现确定性的 `TechnicalAnalysisAgent`，先计算收益率和简单移动平均线，不接 LLM。

[2026-08-04] 第 4 周技术分析 Agent 完成
- 将离线模拟行情扩充为 30 根日线，覆盖 2026 年 6 月 22 日至 7 月 31 日，全部标注为 `synthetic_fixture`。
- 新增 `TechnicalAnalysisAgent`，通过现有 Harness 计算最新单日收益率、SMA5 和 SMA20。
- 输出 `bullish`、`bearish` 或 `mixed`，并附带触发规则、样本数量、数据时间和来源；结果明确不构成投资建议。
- 数据不足 20 根或 `market_data` 类型错误时抛出稳定错误，Harness 会保留失败 trace 和原始原因。
- 新增可直接运行的 `Scripts/demo_technical_analysis.py`，终端可查看结构化指标和 Harness trace。
- 验证结果：新增 5 项 Agent 与演示测试，项目共 35 项 unittest 全部通过；源码、测试和脚本编译检查通过。

下一步
- 把离线行情读取、技术分析和结果汇总接成最小 Graph 工作流，验证金融模块与阶段一平台骨架的组合。

[2026-08-04] 任务书对齐修正
- 复核后确认，先前的“周任务完成”表示内部原型里程碑完成，不能直接代表任务书正式任务完成。
- 建立 `ROADMAP.md`，统一记录正式任务、完成定义、阶段门槛和修正版第 5 至第 19 周计划。
- `checklist.json` 改为 T1.1 至 T4.3 的正式任务口径，同时保留已经完成的 W0 至 W4 内部里程碑证据。
- 当前真实状态：T1.1 完成；T1.2、T1.3、T1.4、T2.1、T2.2 进行中；阶段三和阶段四尚未开始。
- README 和 SPEC 已撤回“阶段一完成”的表述，历史记录不删除，但以后不再用周数替代正式验收。

下一步
- 暂缓最小金融 Graph，先完成 T1.4 的五类 Guardrail，并按总纲通过阶段验收后再推进。

[2026-08-04] 最终交付目标补全
- 重新核对任务书正文、表格和最终 Checklist，把计划从周数驱动改为最终成果和验收证据驱动。
- 补入此前遗漏的非金融 Demo、真实 LLM Model Gateway、Hook 事件循环、Graph 边 Schema 和 LangGraph 映射。
- 明确四类金融 Agent 的指标与输出、2–3 轮辩论协议、风控数值、人工确认和 20 只股票端到端交付。
- 明确夏普基线、可观测指标、Harness 对比实验、1–2 周模拟运行和最终文档清单。
- 保留并强化现有优势：Decimal、source/timestamp/as_of、离线 fixture、失败 trace、Checkpoint、信号/执行时间分离和真实交易关闭。
- `checklist.json` 增加平台、金融应用和工程化三项最终验收结果，避免局部任务完成后误判整个项目已经完成。

下一步
- 当前仍先完成 T1.4 的五类 Guardrail；后续实现顺序可按依赖调整，但最终验收项不得省略。

[2026-08-04] 任务 1.4 Harness SDK 完成
- 实现 JSONSchemaValidator、SourceAttributionFilter、RateLimiter、KeywordBlocker 和 CrossValidator 五类内置 Guardrail。
- 新增 GuardrailRegistry：可以从统一配置创建五类规则，也支持注册自定义 Guardrail 插件。
- Harness trace 现在记录每条规则在输入和输出阶段的开始、通过或失败；配置错误与运行时违规使用不同错误类型。
- JSON Schema 使用明确的确定性子集，不支持的关键字会在配置阶段拒绝；限流器明确为单进程滑动窗口，不冒充分布式限流。
- 新增离线演示，展示五类规则全部通过、误导性关键词在输出阶段被拦截，以及超额调用在输入阶段被限流。
- 验证结果：新增 15 项 Guardrail 和演示测试，项目共 51 项 unittest 全部通过；源码、测试和脚本编译检查通过。

下一步
- 继续完成 T1.2，先实现规划、行动、观察、反思和受控工具调用闭环。

[2026-08-04] 任务 1.2 认知 Loop 与受控工具切片完成
- 新增 Plan、Action、Observation 和 Reflection 稳定契约，Reflection 明确区分继续、修正和完成。
- 新增 ToolRegistry 工具允许列表；未知工具不能执行，每次 Action 的参数和工具结果都经过 Harness 前后校验。
- 工具异常或 Guardrail 拒绝会形成失败 Observation，Agent 可以根据真实失败修正下一步；同一 Action 支持有限重试。
- 新增 CognitiveLoopState、工具执行记录和认知 trace；超过最大步数时安全停止，并保留状态、Harness trace 和原始原因。
- 新增离线计算演示：第一次参数校验失败，Reflection 修正后第二次调用已注册工具并完成。
- 验证结果：新增 7 项认知 Loop 与演示测试，项目共 58 项 unittest 全部通过；源码、测试和脚本编译检查及 checklist JSON 解析通过。
- T1.2 仍为进行中：工作/项目/组织记忆、三类调度、任务隔离、上下文注入、Model Gateway 和非金融 Demo 尚未完成。

下一步
- 为认知 Loop 增加有容量边界、可 JSON 快照恢复的工作记忆，再逐步扩展项目记忆和组织记忆。

[2026-08-05] A2 / 任务 1.3 Graph Engineering 完成
- 新增版本 1 YAML/JSON 工作流加载器和 NodeRegistry；配置只能绑定允许列表中的处理函数，条件使用固定操作符，不执行任意表达式。
- 每条 GraphEdge 强制声明 source 输出 Schema 和 target 输入 Schema，复用 Harness SDK 的确定性 JSON Schema 子集做传递校验。
- GraphRunner 支持顺序或并行拓扑波次；并行节点读取同一状态快照，按声明顺序合并，重复写入同一状态键会明确拒绝。
- 新增节点级有限重试和软超时；新增 closed/open/half_open 熔断状态，并把节点尝试次数和熔断状态写入版本 2 Checkpoint。
- 保留条件分支、跳过传播和故障恢复；并行波次中已成功节点可以进入 Checkpoint，恢复时不重复执行。
- 新增 GraphVisualizer，可输出静态 DAG 或按 pending/completed/skipped/failed 着色的 Mermaid。
- 新增自研 Graph 与 LangGraph 的概念映射，明确当前不引入 LangGraph 运行依赖。
- 新增完整离线演示：从 YAML 加载两个并行分析节点，其中一个节点首次失败后重试，最终输出 Checkpoint、熔断状态和 Mermaid 文件。
- 环境验证：项目使用 D:\Anaconda\python.exe（Python 3.13.2）；PyYAML 6.0.3 已安装到该解释器并写入 pyproject 依赖。
- 验证结果：新增 11 项 Graph Engineering 与演示测试，项目共 69 项 unittest 全部通过；源码、测试和脚本编译检查及 checklist JSON 解析通过。
- T1.3 已达到总纲 A2 的完成口径，checklist 状态更新为 done，remaining 为空。

下一步
- 返回当前主线 T1.2，完成有容量边界、可 JSON 快照恢复的工作记忆最小闭环。

[2026-08-05] 任务 1.2 工作记忆最小闭环完成
- 新增稳定的 MemoryEntry、WorkingMemoryView 和版本 1 WorkingMemorySnapshot 契约，只接受可序列化、有限数值的 JSON 数据。
- 新增容量固定的 WorkingMemory；容量满后按 FIFO 淘汰最旧条目，并持续记录已淘汰数量和单调递增序号。
- CognitiveLoopRunner 会把 Plan、Action、Observation 和 Reflection 摘要写入工作记忆；Agent 在选择动作和反思时读取同一份不可变视图。
- 新增统一 WorkingMemoryStore 接口以及内存、JSON 两种实现；JSON 使用临时文件替换，支持跨运行恢复并拒绝损坏或不兼容的快照。
- 新增离线纠偏演示：第一次参数校验失败，Agent 根据工作记忆修正参数；容量 5 时淘汰最旧两条，随后从 JSON 快照恢复最近条目。
- 保留完整 CognitiveLoopState 历史、最大步数、有限重试、Harness 失败 trace 和安全停止；工作记忆不替代审计历史。
- 验证结果：新增 7 项工作记忆与演示测试，项目共 76 项 unittest 全部通过；源码、测试和脚本编译检查、依赖检查及 checklist JSON 解析通过。
- T1.2 仍为进行中：项目/组织记忆、三类调度、任务隔离、上下文注入、Model Gateway 和非金融 Demo 尚未完成。

下一步
- 为 T1.2 增加按 project_id 隔离、可跨 Loop 运行持久化的项目记忆最小闭环。

[2026-08-05] A3 / 任务 1.2 Loop Engineering 完成
- 新增 LongTermMemory，以项目和组织命名空间隔离长期事实、决定、产物和约定；支持显式 upsert、筛选查询、受控删除、revision、来源和带时区时间。
- 新增内存与 JSON 长期记忆适配器；项目和组织记忆可以跨 Loop 运行恢复，损坏、重复或版本不兼容快照会被拒绝。
- 新增 ContextInjector，把调用方明确选择的 Skill、项目约定、任务上下文、项目记忆和组织记忆组合成只读 InjectedContext；未选择的长期记忆不会自动注入。
- CognitiveLoopRunner 会把同一注入上下文提供给 Planner、Action 和 Reflection，同时继续保留有界工作记忆、最大步数、工具重试、失败 trace 和安全停止。
- 新增 TaskWorkspaceManager；每次调度运行使用独立持久目录，任务 ID、绝对路径和 `..` 路径逃逸会被拒绝；本地目录写入已忽略的 `.runtime/`。
- 新增 HeartbeatLoop 和五字段 CronLoop，按时间槽或 Cron 分钟生成持久幂等键；宿主通过确定性 tick 驱动，不在库内部启动永久后台线程。
- 新增 HookLoop，按订阅名称过滤事件，并用 hook_id + event_id 防止重复处理；事件 payload 作为 JSON 兼容任务上下文注入。
- 新增 GoalLoop，按子目标后序执行和汇总，使用 max_depth、max_goals 硬限制递归规模，并通过 run_id + 目标路径恢复已完成目标。
- 三类触发方式共用 LoopDispatcher 和版本 1 LoopRunLedger；记录成功或失败、执行时间、触发来源和工作目录，JSON 损坏或版本不兼容时拒绝恢复。
- 新增 A3 综合离线演示：Heartbeat、Cron、Hook 和三个递归目标节点共执行 6 次 Cognitive Loop，形成 6 条运行记录和 6 个独立工作目录。
- 验证结果：新增 19 项长期记忆、上下文、工作目录、调度和演示测试，项目共 95 项 unittest 全部通过；源码、测试和脚本编译检查、依赖检查及 checklist JSON 解析通过。
- 任务 1.2 已满足 ROADMAP A3 的完成口径并标记为 done；A4 Model Gateway 和 A5 非金融 Demo 单独保留为平台交付缺口。

下一步
- 进入 A4：先实现确定性 Mock Model Gateway 的稳定请求、响应、错误和追踪契约，再做一种真实 LLM 的最小受控验证。

[2026-08-06] A4 统一 Model Gateway 离线实现完成
- 新增稳定的 ModelRequest、ModelResponse、ModelUsage、ModelRetryPolicy、ModelTraceEvent 和归一化错误契约；成功与失败都会保留模型、token、耗时、尝试次数和有序 trace。
- 新增确定性 MockModelAdapter，支持固定结果和脚本化故障，可离线验证结构化输出、重试、超时与失败路径。
- 新增 OpenAIResponsesAdapter，使用 Responses API，把统一请求转换为 input 和 text.format JSON Schema，并解析实际模型、响应 ID、output_text 与 token 用量。
- Gateway 会对结构化结果进行本地 JSON Schema 二次校验；临时限流、超时、连接故障和 5xx 可有限重试，认证、权限、额度、无效请求、拒答和错误响应立即失败。
- OpenAI 密钥只从 OPENAI_API_KEY 读取；默认演示和全部单元测试不联网、不消耗额度，真实请求必须显式使用 --live。
- A4 定向验证 15 项、项目完整回归 110 项测试全部通过；源码和脚本编译、checklist JSON、Git diff 格式检查通过，离线演示成功输出 provider、model、status、token、latency、attempts、structured_output 和 trace。
- A4 仍为进行中：本机未配置 OPENAI_API_KEY，尚缺一次最小真实 Responses API 调用及实际字段核对，未提前标记为 done。

下一步
- 用户在本地临时设置 OPENAI_API_KEY 后，运行 `python Scripts\demo_model_gateway.py --live --model gpt-5.6-luna`，完成 A4 最后一项真实验证。

[2026-08-06] A4 增加 DeepSeek 真实模型适配器
- 根据 DeepSeek 当前官方接口新增 DeepSeekChatAdapter，使用 Chat Completions、DEEPSEEK_API_KEY 和 deepseek-v4-flash；不再使用已经过弃用日期的 deepseek-chat 默认名。
- DeepSeek JSON Output 只保证合法 JSON，适配器会把目标 Schema 写入 system prompt，返回后继续由统一 Gateway 做本地 JSON Schema 严格复核。
- 将 DeepSeek 的 prompt/completion/total token、实际模型名、响应 ID 和 finish_reason 转换为平台统一契约。
- 将 401、402、429、500/503 分别归一化为认证、额度、限流和服务故障；只有临时故障进入有限重试。
- 抽取模型适配器共用的 JSON HTTP 实现，OpenAI 与 DeepSeek 继续复用同一 ModelGateway interface，Loop、Graph 和 Harness 不需要因供应商变化而修改。
- 新增 DeepSeek 离线适配器测试和无密钥演示测试；所有测试使用假的环境字典和传输层，不访问真实接口。
- 验证结果：A4 相关 21 项、项目完整回归 116 项 unittest 全部通过；源码和脚本编译、checklist JSON、Git diff 格式检查及默认离线演示通过。

下一步
- 用户在本地临时设置 DEEPSEEK_API_KEY 后，运行 `python Scripts\demo_model_gateway.py --live --provider deepseek --model deepseek-v4-flash`，完成 A4 最后一项真实验证。

[2026-08-06] DeepSeek 首次真实调用与语义校验修正
- 用户真实调用成功：provider=deepseek、model=deepseek-v4-flash、attempts=1、input/output/total token=123/13/136、latency=974ms，证明网络、认证、模型和用量字段映射均可用。
- 返回的结构化结果为 mode=offline，与真实运行事实不一致；原 Schema 同时允许 offline 和 live，因此格式校验通过但语义校验没有拦截。
- 新增红色回归测试，确认 live Schema 原本允许 offline；随后改为按运行模式动态生成 Schema，live 只接受 live，offline 只接受 offline，并把提示词改为明确固定值。
- 演示新增 response_id 输出，便于核对真实供应商响应；A4 暂不提前完成，等待修正后的最小真实请求复验。
- 修正后项目完整回归 118 项 unittest 全部通过，源码和脚本编译、checklist JSON 与 Git diff 格式检查通过。

下一步
- 在同一个已设置 DEEPSEEK_API_KEY 的 PowerShell 中重新运行真实演示；输出必须包含 mode=live 和非空 response_id。

[2026-08-06] A4 统一 Model Gateway 正式完成
- 修正后的 DeepSeek 真实调用成功：provider=deepseek、model=deepseek-v4-flash、response_id=b78413b7-fb38-4621-ac51-4ba3eb0925e2、status=succeeded、attempts=1。
- 实际 input/output/total token 为 116/12/128，耗时 1063ms；结构化输出 mode=live，通过收紧后的本地 Schema 校验。
- A4 已满足统一 Mock/真实模型接口、结构化输出、超时、有限重试、错误归一化、调用追踪、环境变量密钥边界、离线测试和最小真实验证全部条件，checklist 标记为 done。
- 验收证据不保存 API Key 或原始响应缓存，只记录非敏感运行字段；真实交易边界没有变化。

下一步
- 进入 A5 非金融通用性 Demo，复用现有 Harness、Loop、Graph、Model Gateway、记忆和受控工具接口证明平台可跨领域接入。

[2026-08-06] A5 非金融通用性证明完成
- 新增 `agent_platform.research` 独立领域模块，包含本地资料契约、确定性检索工具、Model Gateway 认知 Agent、证据综合 Agent 和工作流装配；A5 没有修改 core 框架语义。
- 认知 Loop 使用 Plan、Action、Observation、Reflection 和有界工作记忆，只能通过 ToolRegistry 调用 `local_document_search`；工具结果经过 JSON Schema 和 source/timestamp/as_of 校验。
- 新增三节点 YAML Graph：retrieve 负责 Loop 检索，organize 用确定性代码生成证据编号，synthesize 通过 AgentHarness 生成报告；两条边均声明输入输出 Schema。
- 报告 Harness 同时检查结构、来源、主题一致性和证据编号；不存在的 evidence_id 会被 CrossValidator 拒绝，模型不能绕过确定性来源约束。
- 默认演示使用四次脚本化 Mock 调用，不联网、不消耗额度；保留显式 `--live` DeepSeek 模式，真实调用继续只读取当前终端的 DEEPSEEK_API_KEY。
- 新增故障恢复演示：synthesize 首次失败后从版本 2 Checkpoint 恢复，恢复阶段节点调用为 retrieve=0、organize=0、synthesize=1，已完成节点没有重复执行。
- A5 定向验证 10 项、项目完整回归 128 项测试全部通过；源码和脚本编译、checklist JSON、YAML 工作流解析、Git diff 格式和运行文件忽略规则检查通过。
- 离线恢复演示成功输出三节点完成状态、1 步认知 Loop、1 条带来源证据、4 次模型调用、128 个模拟 token 和完整 Graph trace。
- 新领域接入在 2026-08-06 的单次开发阶段内完成，低于两天验收上限；接入步骤和限制记录在 `docs/non-financial-research-demo.md`。
- A5 和 PLATFORM 已在 checklist 标记为 done；这只代表通用平台交付包完成，不代表金融应用和工程化任务已经完成。

下一步
- 进入 B1 金融数据 MCP，先做一个真实金融数据接口的最小字段、时间、权限和错误验证，再定义统一工具契约与离线回放。

[2026-08-06] 交付包一严格验收缺口修复完成
- 复核发现 checklist 曾声明存在 dev-map，但仓库实际缺失；Skill、MCP、SubAgents 也只有占位说明，Echo 只有单元测试而没有独立运行入口，因此此前 PLATFORM=done 的判断过早。
- 新增根目录 `dev-map.md`，逐项连接 SPEC、Rule、Skill、Workflow、Scripts、MCP、SubAgent、dev-map 和任务看板，并记录核心 module 的稳定 interface、adapter 和验证入口。
- 为 Skill、MCP 和 SubAgents 新增版本 1 JSON catalog；active 条目记录 interface、implementation、import_path 和 evidence，pending 金融市场数据 MCP 明确不冒充完成。
- 项目引导测试现在会导入每个 active 条目的真实实现符号，并核对 implementation 和全部 evidence 文件存在，管理证据不再只靠文字声明。
- 新增 `Scripts/demo_echo.py` 和 subprocess 测试；演示通过 AgentHarness 原样返回任务，并输出 preflight、agent 和 postflight 完整 trace。
- 更新三个目录的角色说明、根 README、docs 索引、SPEC、ROADMAP 和 checklist；PLATFORM=done 现在有可核查证据支撑。
- 验证结果：新增 3 项自动化检查后，项目完整回归 131 项全部通过；Echo 独立演示、源码/脚本编译、四份 JSON 解析全部成功。
- 本次只修复交付包一证据，没有实现 B1 真实金融数据 MCP，也没有修改 Harness、Loop、Graph、Memory 或 Model Gateway 的核心 interface。

下一步
- 进入 B1 金融数据 MCP，先运行一个最小真实数据请求并检查返回字段、时区、权限和错误，再冻结 adapter interface。

[2026-08-07] B1 腾讯 A 股日线首切片完成
- 本机确认 AKShare 1.18.64 和 Pandas 2.3.3；东方财富 `stock_zh_a_hist` 的最小请求分别出现 RemoteDisconnected 和 ProxyError，用户说明当前 IP 可能已被东方财富限制，因此切换腾讯数据源。
- 核对 AKShare 官方文档和已安装源码后，真实调用 `stock_zh_a_hist_tx(symbol="sz000001", start_date="20240102", end_date="20240105", adjust="")` 成功返回 4 根日线。
- 真实返回只有 `date/open/close/high/low/amount` 6 列，与官方文档当前列出的 8 列不同；通过新浪同日数据交叉验证，确认本机版本的 `amount` 实际为按手记录的成交量，乘以 100 映射为股，可能丢失不足一手的尾数。
- 新增 `DailyBarQuery`、`MarketDataFetchPolicy`、`MarketDataFetchResult`、稳定错误码和 provider trace；查询强制市场前缀、不复权、最长 366 天、1–3 次有限尝试。
- 新增 `AkShareTencentDailyAdapter`、`JsonDailyMarketDataAdapter` 和受控 `DailyMarketDataTool`；真实网络与离线 JSON 使用同一 `fetch(query, policy)` interface，调用方不依赖 AKShare/Pandas 字段。
- 每根 K 线保留 `source`、获取时间 `timestamp` 和日线行情时间 `as_of`；腾讯只给日期，因此项目明确映射为 Asia/Shanghai 当日 15:00。
- 保存 4 根已验证真实历史日线作为离线回放样本，默认演示不联网；显式 `--live --attempts 1` 的整链验证成功输出 4 根日线和 started/succeeded/mapped trace。
- 新增 6 项 adapter 与演示测试，项目完整回归 137 项全部通过；源码、测试和脚本编译、四份 JSON 解析、离线演示与 Git diff 格式检查通过。
- 完整 B1 仍为 in_progress，尚缺其他频率、实时行情、资金流、财务/宏观/舆情、缓存、限流、Tushare 和远程 MCP 服务。

下一步
- 在腾讯日线 Tool 上增加缓存和限流，减少外部请求和封 IP 风险，再扩展周线或实时行情。

[2026-08-07] B1 统一金融 Data Hub 主体实现完成
- 在腾讯日线首切片之上新增统一 `FinancialDataHub.fetch(dataset, params, mode)` 和受控 `FinancialDataTool`；调用方只面对一个稳定入口，不需要理解每个供应商不同的函数和字段。
- 数据范围扩展到 19 个 dataset：日线、周线、分钟线、实时报价、资金流，资产负债表、利润表、现金流量表、财务指标、估值，指数、行业、GDP、Shibor、LPR，以及新闻、公告、研报和 Tushare 日线。
- AKShare 真实整链验证已覆盖腾讯日线/周线/实时报价、新浪分钟线、同花顺资金流、三大财务报表、ROE 与资产负债率、PE/PB/PS、指数、行业、GDP、Shibor、新闻和巨潮公告；东方财富不可用的链路没有继续作为默认依赖。
- 所有规范化记录统一保留 `source`、获取完成时间 `timestamp` 和数据对应时间 `as_of`；价格、金额和比例进入平台后使用 `Decimal`，不把 Pandas/NumPy 类型泄漏给 Agent。
- 增加按 dataset 设置 TTL 的 JSON 缓存、按 provider 滑动窗口限流、有限重试、可终止子进程硬总超时和稳定错误码；缓存命中不会再次访问外部接口，超时进程会被操作系统终止。
- 增加版本 1 离线 fixture，默认演示可在无网络环境回放全部 19 个 dataset；其中尚未授权的三个 Tushare 样本明确标注为 `synthetic_tushare_contract_fixture`，不会冒充真实数据。
- 使用官方 MCP Python SDK 新增只读 stdio Server，提供 `list_financial_datasets` 和 `get_financial_data` 两个工具；没有注册下单或真实交易能力。
- Tushare adapter 已接入统一 Hub；本机未配置 `TUSHARE_TOKEN` 时，日线、LPR 和研报请求均被归一化为 `auth_required`，证明错误边界有效，但不能算真实成功证据。
- 验证结果：B1 新增 9 项定向测试、项目完整回归 146 项测试全部通过；源码、测试和脚本编译、四份 catalog/checklist JSON 解析、19 类离线演示和 Git diff 格式检查均通过。
- B1 当前仍为 `in_progress`：代码主体和 AKShare 真实链路已经完成，严格验收只剩配置本地 Tushare token，成功运行三项真实请求并替换三个 synthetic fixture。

下一步
- 在不提交密钥的前提下临时设置 `TUSHARE_TOKEN`，完成 `tushare.daily`、`macro.policy_lpr`、`sentiment.research` 的最小真实验证；通过后再把 B1 标记为 done 并进入 B2。

[2026-08-07] B1 Tushare 权限诊断与免费真实来源补齐
- Codex 重启后确认可以读取 `TUSHARE_TOKEN`，且不会输出或保存 token 内容；Tushare 客户端初始化成功，证明 token 本身有效。
- `tushare.pro.daily`、`tushare.pro.shibor_lpr` 和 `tushare.pro.report_rc` 均返回“没有接口访问权限”，说明失败原因是账号权限而不是认证、网络或无数据。
- 修复供应商错误归一化：新增稳定错误码 `permission_denied`，识别中文访问权限、英文 permission denied/forbidden 等提示；权限错误立即失败且不进入无意义重试。新增回归测试先复现旧代码误报，再验证修复。
- 核对 Tushare 官方权限说明：日线和 LPR 当前最低 120 积分，券商研报属于单独权限。为避免让教学项目依赖付费研报接口，将 LPR 改为 `akshare.macro_china_lpr`，将研报改为 `akshare.stock_research_report_em`。
- AKShare LPR 真实整链成功返回 2026-07-20 的一条记录；平安银行研报真实整链成功返回 2 条记录，最新报告日期为 2026-04-26。两项都保留 `source`、`timestamp` 和 `as_of`。
- 用真实 LPR 和研报记录替换对应 synthetic fixture；19 个 dataset 中现在有 18 个真实最小样本，只剩 Tushare 日线明确标为 `synthetic_tushare_contract_fixture`。
- 验证结果：B1 定向 10 项、项目完整回归 147 项测试全部通过；源码/测试/脚本编译、五份 JSON 解析和 19 类离线演示通过。
- B1 仍保持 `in_progress`：严格验收只剩账号取得至少 120 积分后成功调用一次 Tushare 日线，并替换最后一个 synthetic fixture。

下一步
- 在 Tushare 权限中心确认或补足日线所需积分；权限生效后重新运行 `tushare.daily` 最小请求，完成 B1 最后一项验收。

[2026-08-07] B1 金融数据基础设施正式完成
- Tushare 日线权限生效后，统一 Data Hub 真实调用 `tushare.pro.daily` 成功；查询 `000001.SZ` 在 2026-08-01 至 2026-08-07 的数据，返回 4 条日线，调用只尝试 1 次。
- 真实记录覆盖 2026-08-03 至 2026-08-06，包含 open、high、low、close、pre_close、change、pct_chg、vol 和 amount；每条记录统一保留 `source=tushare.pro.daily`、抓取时间 `timestamp` 和收盘时间 `as_of`。
- 用 4 条真实 Tushare 日线替换最后一个 `synthetic_tushare_contract_fixture`。当前 19 个 dataset 全部具有真实最小样本，默认离线演示不需要网络或 token 即可复现其数据契约。
- MCP catalog 中完整金融市场数据条目从 pending 改为 active，记录 Tushare 的日期、代码、查询区间和返回条数，不保存 token。
- checklist T2.1 状态改为 `done`，remaining 清空；ROADMAP 当前主线切换到 B2 四类专业分析 Agent。B1 完成不代表整个金融应用已经完成。
- 最终验证：B1 与项目引导定向 18 项测试通过，项目完整回归 147 项测试全部通过；源码/测试/脚本编译、五份 JSON 解析、19 类离线演示和 Git diff 格式检查通过。

下一步
- 进入 B2：先让现有技术分析 Agent 消费统一 Data Hub，再补齐指标集合、认知 Loop、Harness 校验、真实样例报告和 Graph 节点接入。

[2026-08-07] B2 技术分析 Agent 完成
- 将原有只计算 SMA5、SMA20 和单日收益率的原型扩展为确定性指标引擎，新增 SMA10、MACD、RSI14、KDJ9、布林带、20 日支撑阻力和七项可解释信号评分。
- 新增 `TechnicalAnalysisRuntime`：通过 B1 `FinancialDataTool` 获取 `market.daily`，统一完成行情契约转换、认知 Loop、Harness 校验、独立运行和 Graph 节点接入。
- 技术 Agent 的 Loop 只允许调用 `technical_market_analysis`，最多两步；Harness 使用 JSON Schema、来源字段检查和原始 K 线完整重算，篡改指标或评分会被拒绝。
- 使用 60 根腾讯真实历史日线完成最小真实验证，并保存其中最近 30 根为独立离线 fixture；默认演示可离线复现，不把样本冒充实时行情。
- 新增专项文档与固定样例报告，更新 README、SPEC、ROADMAP、checklist 和 dev-map。
- 定向测试 10 项、项目完整回归 152 项、源码编译、三份关键 JSON 解析、离线演示和 Git diff 检查全部通过；B2 正式任务 T2.2 仍为 `in_progress`，剩余基本面、行业和大盘/宏观三个 Agent。

下一步
- 实现基本面 Agent，接入三大报表、财务指标和估值数据，并补确定性计算、Loop、Harness、测试、报告和 Graph 节点。

[2026-08-07] 技术分析演示输出本地化
- 将技术分析终端演示改为中文名称并保留英文缩写/字段名，例如“相对强弱指标（RSI14）”“布林带（BOLL）”和“综合信号评分（signal）”。
- 同步翻译趋势、评分规则和 Loop/Harness 标题；内部 JSON 字段、Runtime 接口、Graph 状态和校验逻辑保持不变。
- 演示专项 10 项与项目完整回归 152 项测试全部通过。

[2026-08-07] B2 基本面分析 Agent 完成
- 先对平安银行 `fundamental.balance_sheet`、`fundamental.income_statement`、`fundamental.cash_flow`、`fundamental.indicators` 和 `fundamental.valuation` 做最小真实只读验证，确认三大报表关键字段、ROE/负债率/增长、PE/PB/PS 和市值均可取到；同时保留腾讯实时价格来源。
- 新增 `FundamentalAnalysisEngine`，使用 `Decimal` 确定性计算资产负债表、利润表、现金流代理值、ROE/ROA、净利润增长、PE/PB/PS、规则估值分位、折现股东收益 DCF、安全边际和六项综合评分。
- 新增 `FundamentalAnalysisRuntime`：一次受控读取六类 Data Hub 数据，经过基本面专属认知 Loop、JSON Schema、六类来源校验和完整结果重算，并支持独立运行和 Graph 节点。
- 明确估值分位不是历史分位，而是规则区间分位；明确银行不直接套用工业自由现金流 DCF，使用带假设和限制说明的折现股东收益代理模型。
- 新增真实样本离线 fixture、基本面演示、固定样例报告和专项文档。离线演示可直观看到资产负债表、利润表、现金流、估值、DCF、安全边际、评分和完整 trace。
- 基本面专项 6 项测试通过；一次真实运行失败先被 trace 定位为 Schema 漏字段和时间字段误校验，修复后重跑通过，失败路径也纳入测试。
- T2.2 仍为 `in_progress`，剩余行业和大盘/宏观两个 Agent。下一步实现行业 Agent。

[2026-08-07] B2 行业与大盘/宏观 Agent 完成，交付包二四类 Specialist 验收闭环
- 行业 Agent 新增 `IndustryAnalysisEngine` 和 `IndustryAnalysisRuntime`，统一读取 `industry.snapshot` 与 `macro.policy_lpr`，确定性计算行业画像、竞争格局、LPR 政策信号、景气度、项目产业链模板、代表股排序和四项评分。
- 大盘/宏观 Agent 新增 `MacroAnalysisEngine` 和 `MacroAnalysisRuntime`，统一读取指数、关联股票资金流、GDP、SHIBOR、LPR 和研报评级，确定性计算指数趋势、资金面代理、研报/市场代理情绪、Market Regime、风险偏好和四项评分。
- 两个 Agent 都通过自己的 Plan-Action-Observation-Reflection Loop 调用单一受控工具；Harness 分别配置 JSON Schema、来源字段校验和完整结果重算，Graph 节点复用同一 Runtime。
- 行业真实模式完整运行成功：数据来源为 `akshare.stock_sector_spot` 和 `akshare.macro_china_lpr`；玻璃行业样本显示 19 家公司、板块涨跌幅 4.3554%、景气度 hot、评分 40，Loop/Harness 全部通过。
- 大盘/宏观真实模式完整运行成功：数据来源覆盖指数、资金流、GDP、SHIBOR、LPR 和研报；沪深 300 五日窗口收益 1.3756%，平安银行资金净流出 2.12 亿元，Market Regime=mixed、风险偏好=low、评分 -15，Loop/Harness 全部通过。
- 默认离线 fixture 保留真实验证样本的 `source`、`timestamp`、`as_of`，可在无网络时复现；资金面明确是关联股票代理，产业链和 Regime 明确是项目规则边界。
- 新增两个专项演示、两个样例报告、十项 Runtime 测试和两项演示测试；行业/宏观定向测试、演示和源码编译均通过。项目完整回归 170 项测试全部通过，checklist JSON 和五份新样例 JSON 解析通过。T2.2 的四类 Specialist 交付条件已满足，下一步进入交付包三。

下一步
- 进入交付包三：用 Graph 编排四类 Agent，增加综合研判、结构化辩论、Trader、Risk Manager 和真实交易关闭边界的端到端验收。

[2026-08-07] C1 四 Agent 并行联合分析第一切片完成
- 新增 `CombinedAnalysisQuery` 和 `CombinedAnalysisRuntime`，把四个已有 Specialist 的查询与运行过程收拢到一个稳定入口。
- Planner 先生成四路查询，Graph 再把技术、基本面、行业和大盘/宏观放入同一并行波次，最后由 aggregate 汇总。
- 联合层复用四个 Specialist 原有的 Data Hub、确定性计算、认知 Loop 和 Harness，没有复制指标、估值或宏观规则。
- 汇总结果保留四份完整报告、原始证据、来源、Loop 轨迹、Graph 状态和执行 trace，并生成可读的四路摘要。
- 新增离线联合演示、固定样例、专项文档、4 项联合 Runtime 测试和 1 项演示测试；缺失 Specialist 结果的拒绝路径也有覆盖。
- 首次运行发现技术 Agent 的证据是 K 线列表而不是对象，已将联合边 Schema 和完整性校验改为兼容合法证据集合；没有放宽 Specialist 自己的校验。
- 本轮只完成四路并行编排和联合底稿，T3.1 仍为 `in_progress`。

下一步
- 在联合底稿之上实现 Claim → Evidence → Reasoning 格式的 Bull/Bear 结构化辩论，并保留每轮证据引用和失败 trace。

[2026-08-08] C1 结构化 Bull/Bear 辩论第二切片完成
- 新增 `StructuredDebateQuery` 和 `StructuredDebateRuntime`，在四 Agent 联合报告上生成 2–3 轮结构化辩论。
- 每轮都输出 Claim、Evidence、Reasoning；第 2、3 轮通过 `counter_to` 回应上一轮对方观点。
- 每条证据都保留 Specialist、报告路径、实际值、source 和 as_of；校验器会重新读取报告字段，拒绝篡改路径或值。
- Bull 固定引用 fundamental/industry，Bear 固定引用 technical/macro，双方各至少覆盖两个 Specialist，避免把单边证据包装成共识。
- 联合演示新增辩论输出和 `--rounds 3` 入口，新增结构化辩论样例和 4 项专项测试。
- 实现过程中发现并修复技术报告 MACD 柱值字段应使用 `histogram` 的映射错误，并通过真实离线运行验证。
- 本轮仍不实现 Synthesis、目标价区间、置信度、Market Regime 门控、Trader 或 Risk Manager；T3.1 继续保持 `in_progress`。

下一步
- 在辩论结果上实现 Synthesis：综合倾向、目标价区间和 0–100% 置信度，并为 Market Regime 门控预留确定性输入。

[2026-08-09] C1 综合研判与辩论正式完成
- 新增 `C1DecisionQuery` 和 `C1DecisionRuntime`，用一个稳定入口串联四 Agent 并行联合分析、2–3 轮结构化辩论、Synthesis、质量检查和 Market Regime 门控；同一 Runtime 也可直接作为 Graph 节点。
- Synthesis 按技术 25%、基本面 30%、行业 20%、大盘/宏观 25% 的透明权重计算综合评分和倾向。离线样本加权评分为 24.75，门控前为 positive，经过震荡市和低风险偏好修正后为 cautious_positive。
- 以技术最新收盘价和基本面实时价格均值作为参考价，由正向证据强度和风险压力确定性生成 Bear 下限、Bull 上限和完整研究区间。离线样本为 10.49 <= 11.22 <= 13.02；这些数值是研究边界，不是未来价格预测。
- 新增 0–100 证据一致性置信度。离线样本为 69 分，含义是来源、结构、正反证据和方向的一致程度，不是 69% 的盈利概率。
- 新增 Consistency Check，检查四份报告证券代码、source/timestamp/as_of、技术价与基本面实时价口径、方向分歧和辩论证据回放；发现硬矛盾时拒绝生成 C1 结果。
- 新增 Bias Detector，要求 Bull 和 Bear 各引用至少两个 Specialist、不得只有单边证据，并要求联合来源具备多样性。
- 新增 Market Regime Gate：熊市仓位上限不超过 10%，震荡市不超过 20%，低风险偏好不超过 15%。当前离线样本把基础上限 30% 降为 15%，`real_trading_allowed` 固定为 false。
- 联合演示现在一次打印四路分析、结构化辩论、综合倾向、目标价研究区间、置信度、质量检查、仓位门控和完整 C1 trace；新增固定结果样例并同步 README、SPEC、ROADMAP、checklist、dev-map 和专项文档。
- C1 专项 17 项测试通过；项目完整回归 187 项测试全部通过；源码编译、4 份关键 JSON 解析、离线演示和 Git diff 检查全部通过。T3.1 正式标记为 `done`。

下一步
- 进入 C2 Trader 与 Risk Manager：把 C1 研究结论转换为买入、卖出或持有的模拟候选信号，并用确定性规则检查单笔风险、行业仓位、总回撤、交易时间、流动性、止损止盈和人工确认。真实交易继续关闭。

[2026-08-09] C2 Trader 模拟候选信号第一切片完成
- 新增 `TraderQuery` 和 `TraderRuntime`，用一个稳定接口消费完整 C1 报告，不复制四 Specialist、辩论、Synthesis 或 Market Regime 的实现。
- Trader 使用确定性阈值生成 `buy`、`sell` 或 `hold`：正向倾向、加权评分不低于 20 且证据置信度不低于 60 时产生买入候选；负向使用对应阈值；其余情况持有/观望。
- 候选结果继承 C1 的目标价研究区间、证据置信度、Market Regime、风险偏好、研究仓位上限、全部来源和四路 `timestamp`/`as_of`。
- 新增 Trader 专属 Harness：JSON Schema 固定输出结构，CrossValidator 根据同一份 C1 证据重算动作、目标、置信度、来源和安全字段；篡改动作会被拒绝。
- 可行动候选只进入 `awaiting_risk_review`，不会创建订单；C1 研究仓位上限超过 10% 时提前标记 `human_confirmation_required=true`。这不是最终仓位，后续仍由 Risk Manager 计算和强制执行。
- `simulation_only=true`、`order_created=false` 和 `real_trading_allowed=false` 由代码强制保持；Trader 同时支持独立运行和 Graph 节点入口。
- 新增离线/真实数据演示、固定摘要、专项文档、5 项 Runtime 测试和 1 项演示测试。离线样本输出 buy 候选、评分 24.75、研究区间 10.49–13.02、置信度 69、仓位上限 15%。
- 真实数据整链验证成功：`mode=live`，平安银行 C1 加权评分 22.25，Trader 输出 buy 候选，研究区间 10.46–12.87，置信度 74，仓位上限 15%，人工确认标记为 true，未创建订单。
- Trader 专项 6 项测试和项目完整回归 193 项测试全部通过；源码编译、JSON 解析、离线/真实演示和 Git diff 检查通过。T3.2 仅标记为 `in_progress`。

下一步
- 实现确定性 Risk Manager，补齐单笔亏损不超过 2%、单行业不超过 30%、总回撤超过 15% 强制减仓，以及交易时间、Market Regime、流动性、止损止盈和人工确认强制检查。

[2026-08-09] C2 Trader 与 Risk Manager 正式完成
- 新增 `RiskContext`，显式承载模拟账户权益、当前和请求目标仓位、其他同业暴露、组合回撤、平均每日成交额、评估时间、止损止盈和人工确认状态；这些场景数据不会冒充真实券商账户。
- 新增 `RiskManagerRuntime`，根据止损距离计算“单笔预计亏损不超过账户权益 2%”的仓位上限，并与 C1 Market Regime Gate、单行业 30% 上限和流动性上限共同取最小值。
- 组合回撤超过 15% 时禁止新增仓位；已有仓位的目标强制降低 50%。熊市/风险关闭环境禁止新增买入，卖出减仓仍可通过。
- 新增 A 股工作日 09:30–11:30、13:00–15:00 交易时段检查，要求 `+08:00` 时间；平均每日成交额至少 1000 万元，目标名义金额不超过成交额 10%。
- 买入必须满足止损价低于参考价、止盈价高于参考价且收益风险比不低于 1.5；批准仓位超过 10% 时必须显式人工确认。
- 风控结果可为 approved、adjusted、pending_human_confirmation、blocked、forced_reduction 或 no_action，并保留十项检查、各仓位上限和预计亏损计算。
- 新增 Risk Manager 专属 Harness Pre-Flight，在 Agent 执行前计算全部十项硬规则；执行后再通过 JSON Schema 和 CrossValidator 重算完整结果，篡改批准仓位会被拒绝。
- 新增 `C2TradingRuntime` 统一串联 Trader 和 Risk Manager，并支持作为 Graph 节点运行；`simulation_only=true`、`order_created=false`、`real_trading_allowed=false` 始终由代码强制保持。
- 完整离线演示在显式人工确认后批准 15% 模拟目标仓位：预计单笔亏损 0.98%、最终行业暴露 20%、收益风险比 2.47，十项检查全部通过，未创建订单。
- 真实市场数据整链验证成功：平安银行 Trader 输出 buy 候选，研究区间 10.46–12.87、置信度 74；Risk Manager 批准 15% 模拟目标仓位，预计单笔亏损 0.98%、最终行业暴露 20%，真实交易关闭。账户和风控上下文仍为显式模拟输入。
- 新增 10 项 Risk Manager 测试和 2 项完整 C2 演示测试；完整项目回归 205 项全部通过，源码编译、三份关键 JSON 解析、离线/真实演示和 Git diff 检查通过。T3.2 正式标记为 `done`。

下一步
- 进入 C3 完整金融 Graph：编排 C1、Trader 和 Risk Manager，增加“大盘看空时跳过买入”等条件边、Checkpoint 恢复，并对不少于 20 只股票输出标准化报告、交易建议清单和 Harness 审计日志。

[2026-08-10] C3 单股票完整金融 Graph 第一切片完成
- 新增 `FinancialGraphQuery` 和 `FinancialGraphRuntime` 深 module。调用方通过一个接口运行完整 C1、Trader、Market Regime 条件路由、Risk Manager/看空阻断和 Finalize；C1 内部继续复用 Planner、四 Specialist 并行 Graph、辩论、质量检查和 Synthesis。
- 顶层 Graph 正常路径为 `c1_research -> trader -> market_route -> risk_manager -> finalize`。离线确认演示输出 cautious_positive、置信度 69、buy 候选、批准仓位 15.00% 和预计单笔亏损 0.98%。
- 新增确定性条件边：Trader 为 buy 且 Regime 为 bearish、bear 或 risk_off 时，`risk_manager` 节点被标记为 skipped，转入 `market_bearish_skip` 并输出 blocked + hold；真实交易仍关闭。
- 未显式提供止损止盈时，Graph 使用 C1 研究区间下沿/上沿作为模拟默认值并记录来源，不会为了获得区间重复执行 C1。
- 最终报告会重算证券代码、模式、条件路线、决策来源和三项安全字段；篡改条件路线会被拒绝。
- 新增单股票演示、专项文档、4 项 Runtime 测试和 1 项演示测试；专项 5 项和项目完整回归 210 项全部通过，源码编译、checklist JSON 与 Git diff 检查通过。T3.3 标记为 `in_progress`，未提前算作完成。

下一步
- 为金融 Graph 接入 JSON Checkpoint，复现中间节点失败并证明恢复时不重复执行已经完成的 C1/Trader 节点；之后完成不少于 20 只股票的批量报告和审计日志。

[2026-08-10] C3 标准化报告改为界面可见并默认不落盘
- `demo_financial_graph.py` 不再只打印摘要：默认在终端展示基本信息、四 Agent、数据来源与时间、完整 Bull/Bear 辩论、Synthesis、Trader 与条件路由、十项风控、Graph 审计、安全边界和输出路径十个部分。
- 信号、状态、路由和风控检查使用“中文（英文代码值）”格式，既方便阅读又保留稳定代码字段。
- 默认运行不写文件，避免连续分析产生大量本地报告；只有显式提供 `--output-dir` 时，才把完整 `result["report"]` 和 Graph/Harness 审计信息分别写入两个 JSON。
- 演示测试同时检查终端关键章节、默认无文件提示、可选两个文件存在且可解析、最终决策与真实交易关闭字段未变化；项目完整回归 211 项全部通过，C3 仍保持 `in_progress`。

[2026-08-10] C3 完整金融 Graph 正式完成
- `FinancialGraphRuntime` 接入可选 JSON Checkpoint。恢复演示让 Risk Manager 首次发生预期故障，恢复后实际调用为 C1=1、Trader=1、Risk Manager=2，证明已完成节点没有重复执行；临时 Checkpoint 自动清理。
- 新增 `FinancialBatchRuntime` 和 `demo_financial_batch.py`。批量层只依赖单股票 Graph 接口，逐只隔离运行；一只失败不会遮蔽其他结果，统一返回标准化报告、交易建议清单和 Graph/Harness 审计记录，默认不生成报告文件。
- 第一次真实 20 只银行股批量运行完成 18、失败 2，由此发现行业快照截断、外部财务指标页失效、无个股研报和瞬时接口异常等真实边界。
- 行业 Agent 改为保留至少 50 个板块的比较集合，避免目标板块排在数据表后部时被截掉；20 只银行股使用数据源真实板块名“金融行业”。
- 财务指标页不可用时，系统从同源资产负债表和利润表确定性计算报告期 ROE、ROA 和负债率，并标注 `derived:` 来源；没有去年同期数据时净利润增速采用明确的中性代理，不制造假同比。
- Data Hub 将“provider returned no records”规范为 `empty_response`。个股确实没有研报时，宏观 Agent 记录 `not_available` 和派生来源，研报情绪贡献 0 分；超时、网站不可用等其他错误仍正常失败。
- 修复后同一批次真实请求 20、完成 20、失败 0，产出 20 份标准化投研报告、20 条交易建议和 20 份 Graph/Harness 审计记录；其中华夏银行和光大银行给出 hold，说明结果并非固定买入模板。
- 新增批量、恢复、空数据语义、行业范围和财务派生测试；项目完整回归 220 项全部通过。`T3.3` 和 `FINANCE_APPLICATION` 标记为 `done`，真实交易继续关闭。

下一步
- 进入 D1 回测系统：先固定股票池、时间区间、基准和参数，明确信号时间与下一可执行时间，再实现佣金、印花税、滑点、复权、停牌和涨跌停约束。

[2026-08-10] D1 回测执行核心第一切片完成
- 新增自研 `BacktestEngine` 深 module，外部只需提交 `BacktestRequest`；module 内部集中处理目标仓位、撮合、资金持仓、成本、权益曲线和绩效指标，没有新增 Backtrader 运行依赖。
- `BacktestSignal.signal_at` 必须匹配一根已经完成的日线 `as_of`。T 日 15:00 信号只能在下一根可交易 K 线 09:30 按开盘价执行，同一根 K 线成交被明确禁止。
- 成交量为 0 时记录 `execution.skipped.suspended` 并等待下一根可交易 K 线；最后一根 K 线信号保留为 `pending_signal`，不会用最后收盘价强行成交。
- 使用 `Decimal` 计算双边佣金、最低佣金、卖出印花税、买卖滑点和 100 股整手；买入检查现金，卖出计算实现盈亏。
- 输出订单信号/执行时间、原始开盘价、含滑点成交价、费用、现金和持仓，并计算总收益率、最大回撤、年化夏普、已平仓次数、胜率、盈亏比和每日权益曲线。
- 新增终端演示，回放 30 根已真实抓取的平安银行日线。两条信号明确标为脚本化机制输入：7月2日收盘信号于7月3日开盘执行，7月31日清仓信号于8月3日开盘执行；总成本111.51元、演示收益率5.8885%、最大回撤0.7097%。
- 单笔人工选择的盈利区间不能证明策略有效，也不能用于验收夏普大于0.5。报告明确标记执行层未使用未来成交数据，但历史滚动 C3 信号尚未接入，因此 T4.1 保持 `in_progress`。
- 新增 7 项回测接口测试和 1 项演示测试，覆盖下一 K 线执行、成本、停牌、末根信号、错误输入、整数配置、空交易指标和真实交易关闭；项目完整回归 228 项全部通过，源码和演示脚本编译检查通过。
- 按“功能必须能直观看到”的统一要求，演示新增中文验收结论区；`ROADMAP.md` 已规定后续每个完成切片都要提供可直接运行、可直接阅读的验收入口，默认只在终端展示而不批量生成文件。

下一步
- 接入按历史时点滚动生成的 C3 目标仓位信号，确保每次分析只能看到当时及以前的数据；随后补复权、涨跌停、固定实验配置、多股票组合和正式夏普基线。

[2026-08-10] D1 回测系统正式完成
- `BacktestSignal` 新增真实生成时间 `available_at`。即使信号绑定前一日收盘，只要实际生成晚于下一交易日 09:30，也不能倒推到已经过去的开盘成交，而是继续等待。
- 新增 `C3DecisionSnapshot`、`PointInTimeEvidence` 和 `C3BacktestSignalAdapter`。每条 Agent 证据同时校验 `source`、`as_of` 和实际可得时间，并重新校验完整金融 Graph；故意放入下一交易日证据会在回测前被拒绝。
- 新增 `TradingSessionConstraint`，开盘前已知的买卖方向权限可以阻止涨停买入或跌停卖出；成交量为 0 的停牌等待继续保留，所有拒绝都有 trace。
- 新增 `CorporateAction`，在未复权真实价格上显式处理现金分红、送股和拆并股，并同步调整现金、股数和表现成本，避免直接使用可能包含未来调整因子的前复权序列。
- 新增 `BacktestExperimentRunner` 和固定配置 `Workflow/examples/d1_backtest_experiment.json`，完成三只股票等资金组合、沪深 300 基准、组合权益曲线、成本与夏普基线判断。
- 通过腾讯接口真实捕获 `sz000001`、`sh600000`、`sh601398` 各 243 根日线，通过 AKShare 捕获沪深 300 指数 243 根日线；统一保存为一个可离线复现的 fixture，不依赖验收时联网。
- 中文总验收运行 30 个滚动 Agent 回放信号并模拟成交 11 次。组合收益率 -0.5408%、最大回撤 0.7754%、年化夏普 -0.8463、沪深 300 同期 14.0904%、超额收益 -14.6312%；总成本 164.41 元，其中佣金 55.00 元、印花税 29.10 元、滑点 80.31 元。夏普没有达到任务书大于 0.5 的目标，系统如实报告且没有用未来数据或事后调参美化。
- 固定回放输入明确不是过去现场运行过的四 Agent 结论；实际四 Agent 策略绩效需要未来持续积累带发布时点的行业、估值、资金流和宏观快照。本轮完成的是 D1 回测系统、C3 接口和无未来门禁，不虚构历史数据。
- 新增 D1 完成态和可视化验收测试；专项 18 项、项目完整回归 238 项全部通过，源码与脚本编译检查、JSON 配置和真实行情 fixture 解析全部通过。
- `T4.1` 已按正式证据标记为 `done`；真实交易继续强制关闭，终端演示默认不生成报告文件。

下一步
- 进入 D2 Harness 工程化加固，先实现统一可观测记录与中文调用链面板，再推进独立 Evaluator、运行级熔断和最小工具权限。

[2026-08-10] D2 统一可观测记录与中文面板第一切片完成
- 新增 `ObservationAdapter.from_execution(...)`，自动识别 Harness、Graph 和 Model Gateway 的成功结果或执行异常，把三套不同 trace 归一化为 `ObservationRecord`；业务 Agent 和既有运行器无需改写。
- 每次记录包含唯一 run_id、运行层、组件、状态、带时区开始时间、整次耗时、有序事件、尝试次数、错误类型/消息和扩展 metadata；失败前 trace 不会丢失。
- Model Gateway 的 input/output/total Token、provider、model 和 latency 被直接保留；Graph 的节点状态、执行顺序、尝试次数和熔断快照被保留。
- 新增 `ObservabilityDashboard.build(...)`，统一计算总运行数、成功/失败数、失败率、Token 总量、平均/P95/最大耗时，以及 Harness/Graph/Model 三层分项；重复 run_id 会被拒绝。
- 新增中文终端验收 `demo_observability.py`，实际执行正常 Harness、预期失败 Harness、两节点 Graph 和确定性 Mock Model。界面展示 4 次运行、成功 3、失败 1、失败率 25%、总 Token 21、三层调用链和失败原因，五项验收全部通过；默认不生成文件。
- 旧 Harness/Graph trace 只有事件先后、没有单事件时间戳，因此统一记录明确标记 `ordered_only`，只展示真实整次耗时，不伪造节点耗时。
- 新增 7 项核心接口测试和 1 项界面演示测试；专项 8 项、项目完整回归 246 项全部通过，源码编译、checklist JSON 和 Git diff 检查通过。T4.2 标记为 `in_progress`，没有把可观测切片完成误报为整个 D2 完成。

下一步
- 实现独立 Evaluator：固定任务集、预期事实和确定性评分规则，使质量评估不依赖被评估 Agent 自我打分；之后再推进运行级熔断告警和 Agent 最小工具权限。

[2026-08-10] D2/T4.2 Harness 工程化正式完成
- 新增独立 `IndependentEvaluator`。固定数据集包含来源约束、结构化完成、工具权限和金融措辞安全 4 个用例；评分只依据预期事实、禁用短语和允许工具，不读取 Agent 自评分。
- 新增运行级 `IndustrialHarness.run(...)` 深 module。连续失败默认阈值为 3，达到后状态变为 open，产生 `agent_circuit_opened` critical 结构化告警；第 4 次请求在 operation 前返回 `circuit_open`，实际业务调用次数保持 3。
- 熔断超过 reset timeout 后进入 half-open，只放行一次探测；成功后关闭并清零连续失败。当前状态为进程内状态，重启后重置；告警由调用方接外部通知，不擅自发送消息。
- 建立 9 个当前 Agent 的集中最小工具权限。四个金融 Loop 和 Research Planner 只允许各自唯一工具，其余不需要工具的 Agent 使用空列表；未知 Agent 默认拒绝。
- 五个实际工具型 Runtime 已把 Agent 名称和权限注册表传给 `ToolRegistry`，在工具注册和每次 dispatch 时复核。越权 `web_search` 演示在 operation 前被拒绝，实际执行次数为 0。
- 完成固定 Harness 对比实验。两组使用同一 4 个任务、数据和脚本化模型首轮输出；无 Harness/有 Harness 的幻觉率为 80%/0%，无效工具调用 1/0，端到端成功率 25%/100%，平均耗时 40ms/86.25ms，Token 80/120，有 Harness 组失败恢复成功率 100%。结果明确不代表真实模型线上质量。
- 新增稳定 Python interface `D2EngineeringRuntime.from_files().run()`、安装后 CLI `agent-platform d2-verify` 和仓库入口 `demo_d2_engineering.py`；默认终端展示，不生成报告文件。
- Harness 配置和评估数据集作为版本化包资源发布，严格校验键、类型、阈值、重复 Agent 和重复 case；代码权限表、包内配置和 `SubAgents/catalog.json` 由测试保持一致。
- 新增 `requirements.lock` 和 GitHub Actions workflow。CI 配置会在 Windows Python 3.11 安装锁定依赖、运行稳定 CLI 和完整测试；远端结果须推送后由 GitHub 实际确认。
- 新增工业 Harness、Evaluator、总 Runtime 和界面专项测试；相关 42 项测试和项目完整回归 258 项全部通过。源码编译、JSON/YAML 配置和 Git 检查通过。
- `T4.2` 已标记为 `done`；真实交易继续关闭。

下一步
- 进入 T4.3：设计可中断恢复的连续模拟运行台账，使用真实行情累计 1–2 周运行、失败、人工确认和复盘证据，再完成最终文档包。

[2026-08-10] D3 Harness 对比实验新增独立可视化入口
- D3 的六项量化实验已随 D2 完成，本次没有重复制造另一套统计逻辑，而是复用 `D2EngineeringRuntime` 新增稳定子命令 `agent-platform d3-compare` 和仓库入口 `demo_harness_comparison.py`。
- 中文界面单独展示幻觉率、无效工具/API 调用、端到端成功率、平均耗时、Token 和故障恢复率，并展开 4 个固定用例的原始 passed、hallucinations、invalid_calls、recovered、latency 和 tokens。
- 直观验收检查同一数据集、六项指标和成本边界，继续明确脚本化离线实验不代表真实模型线上质量；默认不生成文件。
- 新增界面自动化测试，并把 `d3-compare` 加入 GitHub Actions CLI 校验；项目完整回归 259 项全部通过。D3 保持完成态，下一正式任务仍是 T4.3。

[2026-08-10] D4/T4.3 本地持续模拟运行第一切片完成
- 新增 `PaperTradingRuntime` 深 module，把已经通过 C3 Graph 一致性校验的标准报告接入本地模拟撮合；报告只要改变 `simulation_only`、`order_created` 或 `real_trading_allowed` 任一安全字段，就会在撮合前被拒绝并记录失败。
- 离线复现可从 C3 技术报告读取收盘行情；真实运行单独通过腾讯 `market.realtime` 取得执行报价。两种报价都保留 `source`、`timestamp` 和 `as_of`，并检查行情时点不晚于本次执行评估时间。每轮账本同时保留 C3 倾向、置信度、Market Regime、路由、风控状态、预计单笔亏损和来源摘要。
- 模拟成交使用 `Decimal`、100 股整手、双边佣金、最低佣金、卖出印花税和滑点；更新现金、股数、平均成本与已实现盈亏。订单始终标记 `local_simulator` 和 `real_order=false`，没有券商下单接口。
- 新增 `JsonPaperTradingLedger`，在单个版本化 JSON 中集中保存固定 session 配置、账户、每轮运行、失败、人工确认、模拟成交和复盘摘要；原子替换写入、重复 cycle 拒绝和配置漂移检查使进程重启后可以安全续跑。
- 人工确认门禁已接通：C3 要求确认而用户未确认时，账本记录 `missing`，状态为 `pending_human_confirmation` 且不成交；显式确认后才允许本地撮合。
- 连续周期验收只统计 `mode=live`：至少覆盖 7 个日历日且不少于 5 个不同真实行情交易日。离线 fixture 即使运行成功也不能把 T4.3 自动标记完成。
- 新增中文演示 `demo_paper_trading.py`。默认使用临时账本，不生成文件；显式 `--ledger` 才在 `.runtime/` 保存并让后续同 session 继续追加。已实际跑出离线 buy 1300 股、佣金 5.00 元、滑点成本 7.33 元的本地模拟结果。
- 真实最小验证先发现并拦截了“14:30 评估却使用标记 15:00 的当日日线”时间错误，随后改用独立腾讯实时报价。最终取得 11.2900、`as_of=2026-08-10T16:14:27+08:00`；因当时已经收盘，C3 合理输出 hold，本地账本记录 `no_action`，没有绕过交易时段规则伪造成交。
- 已正式启动本地持久 session `d4-live-20260810`，账本位于 Git 忽略的 `.runtime/paper_trading/d4-live-20260810.json`。首日累计 1 个真实行情交易日、1 个 cycle、0 次失败、0 次成交，账户现金 100000.00 元；该本地运行证据不会提交仓库，也不含密钥。
- 新增 6 项核心测试和 3 项界面测试，专项 9 项通过；JSON、源码编译和项目完整回归 268 项全部通过。
- `T4.3` 已从 `pending` 更新为 `in_progress`。尚未满足真实时间条件，也尚未完成最终架构图、Schema、Agent 卡片、数据字典、运行手册和复盘报告，因此没有把 D4 误报为完成。

下一步
- 用持久账本启动真实行情 session，并按交易日持续追加记录；同时准备最终文档骨架和从环境检查开始的一条主验收流程。

[2026-08-11] D4/T4.3 调整后范围正式完成
- 用户明确要求完成 D4 且不再考虑自然时间等待。原“真实行情连续运行 1–2 周”不被伪装成已达成，而是在 Runtime、总纲、清单和最终报告中统一记录为 `waived_not_proven`；目前真实证据仍只有 1 个交易日，项目不声称证明长周期稳定性。
- 新增 `FinalDeliveryRuntime.from_project().run()` 深 module。调用方只使用一个 interface，module 内部完成 Python/项目/依赖/fixture/安全配置检查，实际运行通用 Harness、C3 完整金融 Graph、D1 固定回测、D2/D3 Harness 工程验收和 D4 本地模拟执行，并核对最终文档包。
- 新增稳定 CLI `agent-platform d4-verify` 和仓库入口 `Scripts/demo_final_delivery.py`。中文界面分六区显示环境、五条主要流程、最终文档、本地真实账本、时间豁免和交易安全边界；默认离线且不生成临时报告文件。
- 总入口实测全部通过：C3 批准 buy 15%，D1 组合收益 -0.5408%、最大回撤 0.7754%、夏普 -0.8463，D2 Evaluator 平均分 100、熔断状态 open，D4 离线本地模拟成交后现金 85336.67 元。所有链路保持 `real_trading_allowed=false`。
- 新增 `docs/final-delivery.md`，用一份总文档整合最终架构 Mermaid、顶层和内部 Graph Schema、九个正式 Agent 卡片、数据字典、运行手册、固定回测报告、Harness 对比实验和模拟运行复盘；专项文档继续保存算法与边界细节。
- 更新 `docs/architecture.md`、`docs/README.md`、`docs/paper-trading.md`、`docs/financial-graph.md`、README、SPEC、ROADMAP、dev-map、Scripts 说明、CI 和 checklist，使完成状态、时间豁免和证据路径一致。
- 新增最终 Runtime 正常/失败/文档测试和中文界面测试；专项 4 项通过，JSON、源码编译和项目完整回归 272 项全部通过。仓库脚本和源码路径下的 `agent-platform d4-verify` 均实际返回 0；本机 Anaconda 尚未安装 editable package，因此直接使用安装式命令前仍需执行 README 的 `pip install -e .`。
- `T4.3`、`ENGINEERING` 和项目总状态按调整后范围标记为 `done`。真实交易、保证盈利和长周期稳定性证明仍明确不属于已完成能力。

后续增强（非正式任务缺口）
- 可以继续积累真实模拟账本、增加 Web UI、运行真实模型线上 Harness 对比或扩展更多资产类别。

[2026-08-11] A–D 一体化 Web Control Desk 完成
- 新增 `DashboardRuntime` 深 module，用 `overview()`、`run_action()` 和 `ask_assistant()` 三个稳定接口统一平台展示、白名单任务执行和结果讲解；A1–D4 及最终验收共登记 18 个可视化入口。
- 新增本地 HTTP adapter 和纯静态中文前端。页面用 A→B→C→D 执行轨道展示平台底座、金融数据与四 Agent、联合决策风控、回测与工程验收；每个功能都有简介、模式选择、运行按钮、核心摘要和完整 trace。
- 新增 DeepSeek 项目助手 adapter，复用 A4 `ModelGateway`、结构化 JSON Schema、重试、Token 和耗时记录。助手只接收有界结果摘要，只能解释和推荐白名单 action，不能自动执行，也不能覆盖确定性指标、仓位和风控。未设置 Key 时自动使用本地规则助手。
- 后端只接受登记过的 action id 和固定参数，拒绝任意命令；服务只绑定 127.0.0.1。真实模式只读取数据或调用模型，D4 仍是本地模拟撮合，`real_trading_allowed=false` 保持不变。
- 新增 `Scripts/run_dashboard.py` 和安装式命令 `agent-platform dashboard`。推荐运行 `D:\Anaconda\python.exe Scripts\run_dashboard.py`，不再要求用户记住各阶段脚本。
- 新增 10 项 Dashboard 自动测试，覆盖 A–D 总览、动作白名单、真实模式参数、DeepSeek 结构化 Gateway、助手只建议不执行、HTTP 页面/API 和前端资源，全部通过。
- 实际浏览器验收通过：首屏正确显示 18 个入口和 4 个阶段；切换 B 阶段并点击 B1 后，0.35 秒显示 19 类 Data Hub 离线结果摘要；本地助手读取当前摘要后推荐 C1，但未自动执行；浏览器控制台无错误，375px 手机宽度无横向溢出。

[2026-08-11] 客户证券分析前台与产品命令行总验收完成
- 明确拆分两种使用对象：根路径 `/` 改为面向客户的证券分析 App，`/admin` 保留原 A–D 团队验收后台。客户页不展示 Harness、Graph、Checkpoint 或脚本名，只展示股票研究成果。
- 新增 `ClientAnalysisRuntime` 深 module，复用 C3 完整金融 Graph，把同一份结构化报告投影为 30 根日 K 线、综合观点、技术/基本面/行业/宏观四个维度、支持与风险因素、研究价格区间和确定性风险指标。
- 新增客户分析与解释 HTTP 接口。设置 `DEEPSEEK_API_KEY` 时通过已有 Model Gateway 生成固定 Schema 的通俗说明；未设置时使用本地安全解释。两种解释层都不能修改数值、风险结论或创建订单。
- 新增 `ProductAcceptanceRuntime`、`Scripts/demo_product_acceptance.py` 和 `agent-platform verify-all`。一条命令实际复现 A–D 五条主流程，并检查客户页面资源、四维分析、30 根 K 线、后台 18 个入口和四项交易安全字段；本次离线运行全部通过。
- 新增 `docs/client-app.md`，README、SPEC、ROADMAP、开发地图、脚本索引、文档索引和 CI 均已改为“客户前台 + 团队后台 + 命令行整体验收”的统一口径。
- 当前客户原型只开放平安银行。离线行业 Agent 仍使用现有“玻璃行业”样本，页面明确显示样本范围，未把它冒充成银行行业结论；后续扩展标的时要补齐股票与行业数据映射。

[2026-08-11] 客户前台最新数据与页面状态修复
- 真实模式原先把平安银行映射到“银行行业”，但 AKShare 行业快照的实际 subject 是“金融行业”，导致行业 Agent 找不到目标并在两步 Loop 后终止。映射已改为“金融行业”，与真实数据源和现有产业链规则一致。
- 客户 CSS 增加统一 `[hidden]` 规则，避免 `.loading-state` 和 `.error-state` 的 `display: grid` 覆盖 HTML 隐藏状态。现在分析中、分析成功、分析失败三个界面严格互斥。
- 新增真实模式行业名称和前端隐藏语义回归测试；修复前两项测试稳定失败，修复后通过。

[2026-08-12] 客户前台扩展到 20 只沪深银行股
- 将客户证券目录从单只平安银行扩展到 C3 已真实批量验证的 20 只银行股，覆盖 17 只上交所股票和 3 只深交所股票；前台下拉框统一从该目录生成名称、代码和交易所。
- 每只证券在同一目录内声明可用数据模式和行业映射。20 只股票都支持最新只读数据并使用“金融行业”；只有平安银行拥有完整离线四维样本，因此继续支持已验证快照。
- 选择其余 19 只股票时，页面自动切换到最新数据并禁用快照按钮，避免把平安银行离线财务数据冒充成其他股票。
- 实际使用浦发银行 `sh600000` 完成上交所真实链路验证：60 根 K 线、最新价 9.17、行业范围金融行业、综合观点中性。
- 产品总验收新增“股票池不少于 20 只”和“覆盖沪深两市”；新增目录、模式约束与页面联动回归测试。

下一步
- 当前统一界面已经可用于项目展示。可选增强是加入 K 线图表、把更多结构化指标转成图形组件，以及在专门实验中量化真实 DeepSeek 有/无 Harness 的线上差异。

[2026-08-12] 强化计划：客户四维研究可视化完成
- 对照总纲确认 A–D 正式任务已经完成，本次归类为产品展示增强，不改变任何正式任务状态，也不提前消耗真实 DeepSeek 调用。
- 客户前台新增“四维研究天平”：以 0 分为中轴，负分向左、正分向右，直接比较技术、基本面、行业和宏观四个 Agent 的方向与强度。
- 页面根据既有确定性分数计算主要支持、主要风险和最高最低分差。平安银行离线样本显示经营质量 +60、市场环境 -15、分歧 75 分；没有增加另一套金融判断逻辑。
- 原四维卡片继续保留文字解释，并把卡片内进度条统一改成相同的左右零轴语义，避免负分被看成从左到右的正向进度。
- 产品整体验收新增“四维观点已有直观图形”检查；前端资源测试、JavaScript 语法检查和桌面浏览器实际加载均已通过。
- 375px 手机宽度实际检查无横向溢出，研究天平的摘要、刻度和四条分数条均可见；真实交易边界保持关闭。

下一步
- 可继续实现总纲中的受约束动态大模型辩论，或使用固定线上评测集量化 DeepSeek 有/无 Harness；这两项会产生真实模型调用与成本，应作为独立增强切片推进。

[2026-08-12] 强化计划：受约束动态多空辩论核心切片完成
- 新增 `DynamicDebateRuntime` 深 module。调用方只提交已有 `StructuredDebateQuery`，module 内部完成编号化证据目录、模型候选生成、证据还原、确定性复核、有限重试和固定辩论降级。
- DeepSeek 只能返回两到三轮 Bull/Bear 的 Claim、evidence_ids 和 Reasoning。后端从原四 Agent 报告重新填入 path、value、sources 和 as_of，并复用 C1 `validate_structured_debate` 检查每方至少覆盖两个 Specialist。
- 新增虚构数字和“保证收益/立即买入/已经下单”等措辞拦截。候选最多进行两次语义尝试，仍不合规则返回原固定辩论；Synthesis、目标区间、置信度、仓位和风控完全不由该 module 修改。
- Web 端新增短期 `analysis_id` 门禁，服务器内存最多保存 32 份原始辩论上下文。浏览器不能上传或覆盖证据，未知/过期编号被拒绝；服务重启后上下文自动失效，不生成文件。
- 客户页面新增“生成动态多空解读”按钮。用户手动点击才会调用模型，页面展开两轮多方与空方观点，并显示模型、Token、复核或降级状态；普通分析不会增加该功能的 Token 成本。
- 5 项动态辩论测试覆盖合规候选、未知证据、无 Gateway、虚构数字和保证收益措辞；HTTP 测试覆盖有效编号和伪造编号。专项 27 项通过，真实交易保持关闭。
- 当前 Codex 进程检测结果为 `deepseek_configured=False`，因此本次只完成 Mock 与无 Key 降级验证，不把真实 DeepSeek 调用或固定线上评测集误报为完成。该增强保持 `in_progress`。

下一步
- 在能够读取 `DEEPSEEK_API_KEY` 的 PowerShell 中完成一次真实动态辩论，再建立固定线上评测集统计证据有效率、观点多样性、正反平衡、重试率、耗时、Token 成本和稳定性。

[2026-08-13] Web 启动时安全输入 DeepSeek Key
- 新增统一启动配置模块。两个入口 `Scripts/run_dashboard.py` 和 `agent-platform dashboard` 在当前进程没有 Key 时都会隐藏提示输入；已有环境变量时直接复用，不重复询问。
- 输入的 Key 只注入本次服务进程，不显示、不写入 `.env` 或仓库，关闭服务后失效；直接回车则继续使用本地安全解释与固定多空辩论。
- 提示发生在 Web Runtime 创建之前，因此同一个 Key 会同时启用客户智能解读、动态多空辩论和后台项目助手。使用测试占位值完成进程级初始化检查，三个组件均识别为 DeepSeek 模式，未发起网络请求。
- 新增 `--no-key-prompt`，供自动化或无人值守启动跳过交互；无环境变量时安全退回固定格式。
- 新增 5 项启动专项测试，并把隐藏输入与本地降级加入产品命令行整体验收。真实 Key 的网络调用仍由用户在本地受控验证，不在代码或测试中保存密钥。

下一步
- 用户运行统一入口，按提示输入自己的有效 DeepSeek Key，再在客户页验证一次智能解读和动态多空辩论；固定线上评测集仍是动态辩论增强项的剩余工作。

[2026-08-13] 修复多股票最新数据偶发内部错误
- 使用与客户网页相同的 `ClientAnalysisRuntime` 稳定复现浦发银行和招商银行失败，异常定位到基本面 Agent 的 `fundamental.valuation`，并非日线、Tushare Token、股票代码或整套真实数据不可用。
- 腾讯实时报价时间实测比本机时间快约 2–4 秒；原来源校验要求 `as_of` 绝不能晚于抓取时间，导致正常实时估值被误判为未来数据。网络耗时偶尔抵消时又会成功，因此表现为不稳定。
- 只对腾讯实时行情与腾讯参与的实时估值显式允许 10 秒服务器时钟偏差；容差内把记录 `timestamp` 提升到 `as_of`，继续满足统一数据契约。其他历史数据不放宽，超过 10 秒仍拒绝，回测未来数据门禁不变。
- 新增两项回归测试，证明 5 秒偏差通过、11 秒偏差拒绝。真实验证中浦发、招商和平安估值均成功；浦发和招商完整客户分析均返回 60 根 K 线、12 类来源和完整综合结论。
- 随后按客户网页同一条真实完整链逐一验收 20 只股票，首轮暴露个股资金流接口会空响应或偶发超时。宏观 Agent 现仅在 live 模式把 `EMPTY_RESPONSE`、`TIMEOUT`、`PROVIDER_UNAVAILABLE` 或 `RATE_LIMITED` 转成有来源标记的 `not_available` 证据，资金流和相关市场代理固定贡献 0 分，并在 caveat 明示缺失；不会把 0 冒充真实净流入，也不会据此推断单边 Market Regime。
- 最终 20/20 真实完整客户分析通过、失败 0。每只均返回 60 根 K 线、四个 Agent 维度、12 类来源、综合结论和 `real_trading_allowed=false`；总耗时 571.25 秒，单只约 16–63 秒，说明可靠性已恢复但冷启动性能仍有优化空间。

[2026-08-13] 强化计划：动态辩论固定评测与统一展示完成
- 新增 `DynamicDebateEvaluationRuntime.run(dataset)` 深 module，固定使用同一份四 Agent 离线研究底稿，分别执行 2 轮和 3 轮辩论并各重复两次；数据集版本、用例和重复次数集中在 JSON 资源中校验。
- 同时运行固定模板基线与受约束动态辩论，量化候选/最终证据有效率、观点多样性、正反平衡、重试、降级、平均耗时、Token 和重复结果稳定性；终端保留四次逐项原始结果，不再只写“更灵活”。
- 脚本化 Mock 首次故意引用未知证据，证明候选会被拒绝、第二次语义尝试能恢复；最终结果继续由确定性证据重放、双方覆盖和安全字段检查决定。Mock 只验证链路，不冒充真实模型质量。
- 新增 `agent-platform debate-eval`、`Scripts/demo_dynamic_debate_evaluation.py` 和后台 C 阶段“C1+ · 动态辩论量化评测”卡片。默认不生成文件；只有用户显式使用 `--output` 才保存含逐次结果的 JSON。
- 产品整体验收新增动态辩论固定评测入口检查，团队后台从 18 个增加到 19 个操作入口；README、总纲、SPEC、开发地图、客户/后台说明和专项文档已同步。
- 当前运行环境未读取到真实 DeepSeek Key，因此代码、离线评测与展示已完成，增强项仍保留 `in_progress`；剩余外部验收仅为用户执行 `--live --output ...`，不能把 Mock 结果误报成真实 DeepSeek 结论。

[2026-08-13] P1 异步分析任务中心首个纵向切片（历史记录）
- 新增 `AnalysisJobRuntime` 深 module，通过 `submit/get/cancel/result` 四个 interface 隐藏本地线程 Worker、任务容量、状态转换、错误归一化和协作取消；首版最多并行 2 项、内存保存 64 项，不提前引入分布式队列。
- 客户分析 HTTP 改为先返回 `202 Accepted + job_id`，浏览器再查询状态，成功后单独读取报告。旧 `/api/client/analyze` 同步入口暂时保留，避免破坏现有调用方。
- `ClientAnalysisRuntime` 在真实执行点上报“四维联合研究与风险检查、行情图表整理、客户报告”三个阶段；没有用计时器伪造四个 Agent 的完成百分比。
- 客户页新增任务编号、纵向进度轨道、真实阶段状态和“停止本次分析”。任务编号保存在当前浏览器会话，普通刷新后继续查询；成功后原 K 线、四维分析、智能解读和动态辩论流程保持不变。
- 排队任务可以立即取消；运行中任务只在下一安全进度点协作停止，不强杀外部请求、不返回半份报告。任务失败会保留异常类型、可读消息和失败阶段。
- 当时首个切片专项 26 项、项目全量 319 项测试通过；该状态已由下方“P1 正式完成”记录取代。

[2026-08-13] P1 异步分析任务中心正式完成
- `GraphRunner` 新增可选事件 sink，Combined Specialist Graph 与 C3 外层 Graph 在真实节点开始、完成、跳过、重试、超时和失败时直接上报，不改变原有 Graph 结果和 trace。客户任务现在展示 Planner、四 Specialist、证据汇总、C1 辩论/质量/综合、Trader、条件路由、Risk Manager、Finalize、图表和报告共 17 个节点。
- `AnalysisJobRuntime` 外部 interface 增加 `retry(job_id)`；每项任务使用 Specialist/C3 两层 Checkpoint，失败后只执行失败或未完成节点，已完成节点不重跑。HTTP 新增 `POST /api/client/jobs/{job_id}/retry`，客户页失败时显示“只重试失败步骤”和实际尝试次数。
- 任务元数据和成功报告用 UTF-8 JSON 原子保存到 `.runtime/analysis_jobs/jobs.json`，服务启动时自动续跑原 `queued/running` 任务；完整历史检索、迁移和报告比较仍由 P3 SQLite Repository 负责。
- 单任务总时限为 180 秒；超时后状态固定为可重试失败，第三方线程自然返回的迟到结果会被丢弃。取消仍采用安全点协作语义，不强杀阻塞中的 Python 线程。
- 离线完整演示真实跑通 17/17 节点，Risk Manager 路径完成、未选择的弱市阻断路径显示 `skipped`，最终报告保持 `real_trading_allowed=false`。专项测试与产品验收通过，项目全量 324 项测试通过；P1 已在 ROADMAP、SPEC 与 checklist 标记为 `done`，下一项为 P2 统一分析快照。

[2026-08-13] 总纲加入普通版/专业版双层信息架构
- 客户页面首次访问默认普通版，只展示四段通俗进度、结论、主要理由、支持/风险因素、研究区间、数据时间和免责声明；专业版按需展开四 Agent、17 个真实节点、技术指标、来源、评分规则与风控计算。
- 两种模式必须来自同一冻结报告和统一快照，切换只改变展示复杂度，不得重新取数、重新运行 Graph、再次调用 DeepSeek、增加 Token 或产生不同结论。
- P4 调整为“双层展示、专业图表和证据下钻”，由 `ReportViewRuntime.project(report_id, view)` 统一生成 `basic/professional` 投影；P6 历史、比较和导出继续复用该展示规则。
- 专业版客户界面与 `/admin` 工程后台保持分离：专业版展示研究证据，后台保留 Harness、Graph 配置、原始日志和工程验收入口。
