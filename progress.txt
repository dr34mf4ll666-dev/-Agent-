项目进度日志
====================

[2026-07-31] 第 0 周初始化开始
- 已确认项目当前只有任务书文档，没有代码和进度文件。
- 清理了文档阅读产生的临时预览目录，避免污染项目根目录。
- 已建立项目规范、目录职责、功能清单和最小测试入口。
- 当前安全默认值：使用 mock/fixture，不接真实交易。

[2026-07-31] 第 0 周初始化完成
- 骨架测试：5 项全部通过。
- Git：已初始化并创建提交 `6b7769f`。
- 第 0 周交付：规范、目录骨架、配置示例、Python 包入口、离线测试、功能清单和进度日志。

下一步
- 进入阶段一：先实现 Echo Agent，再实现最小 Harness 和 Loop 闭环。

[2026-07-31] 第一周完成
- 定义 `AgentRequest`、`AgentResponse`、`Agent`、`Guardrail` 和 `HarnessResult` 公共契约。
- 实现确定性的 `EchoAgent`，不依赖网络、模型或金融数据。
- 实现 `AgentHarness`：前置检查、Agent 执行、后置检查、可插拔 Guardrail 和有序 trace。
- 失败时保留失败阶段、原始异常和已有 trace；本地文档仍保持不被 Git 跟踪。
- 验证结果：10 项 unittest 全部通过。

下一步
- 第二周实现 Loop 的多步运行、最大步数和失败重试。

[2026-07-31] 第二周完成
- 定义 `LoopState`、`LoopEvent`、`LoopResult` 和 Loop 错误类型。
- 实现 `LoopRunner`：每一步通过 Harness，支持历史上下文、完成条件、最大步数和失败重试。
- 验证正常完成、历史传递、最大步数退出、重试成功、重试耗尽和参数边界。
- 验证结果：15 项 unittest 全部通过。

下一步
- 第三周实现最小 Graph/DAG：节点、条件分支和 Checkpoint 的边界。

[2026-08-03] 第三周完成
- 定义 `GraphState`、`GraphEdge`、`GraphDefinition`、`GraphResult` 和 Graph 错误类型。
- 实现确定性的 DAG 运行器：执行前校验、拓扑顺序、状态合并、条件分支和跳过传播。
- 实现 JSON Checkpoint：节点完成、跳过或失败后保存，恢复时不重复运行已完成节点。
- 验证正常 DAG、环检测、条件分支和失败恢复；本地任务书文档继续保持不被 Git 跟踪。
- 验证结果：19 项 unittest 全部通过，源码和测试编译检查通过。

下一步
- 进入阶段二：先定义带 `source`、`timestamp`、`as_of` 的金融数据契约，再实现第一个离线专业 Agent。

[2026-08-03] Graph 可运行演示完成
- 新增 `Scripts/demo_graph.py`，可以直接从命令行观察节点执行、条件分支、节点状态和共享状态。
- 默认模拟 `recoverable` 节点首次失败，并自动从 JSON Checkpoint 恢复；已完成节点不会重复运行。
- Checkpoint 默认写入已忽略的 `checkpoints/`，不会进入 Git；支持切换分支、关闭故障和自定义存档路径。
- 新增子进程集成测试，验证真实命令、终端输出和最终 Checkpoint；验证结果：20 项 unittest 全部通过，源码、测试和脚本编译检查通过。

下一步
- 进入阶段二的第一个小任务：定义金融数据契约和离线 fixture，暂不接入真实行情 API。

[2026-08-04] 阶段二金融数据契约完成
- 新增 `MarketBar`：统一解析 OHLCV、证券代码、`source`、`timestamp` 和 `as_of`，价格使用 `Decimal`。
- 新增 `MarketDataSeries`：要求序列非空、只包含一个证券代码，并按 `as_of` 严格递增。
- 确定性校验覆盖价格范围、非正价格、负数或非整数成交量、缺失字段、无时区时间、未来 `as_of` 和错误外部格式。
- 新增 5 根明确标注为 `synthetic_fixture` 的人工日线数据，不代表真实证券或投资结果。
- 验证结果：新增 10 项金融契约测试，项目共 30 项 unittest 全部通过；源码、测试和脚本编译检查通过。

下一步
- 使用当前离线数据实现确定性的 `TechnicalAnalysisAgent`，先计算收益率和简单移动平均线，不接 LLM。

[2026-08-04] 第 4 周技术分析 Agent 完成
- 将离线模拟行情扩充为 30 根日线，覆盖 2026 年 6 月 22 日至 7 月 31 日，全部标注为 `synthetic_fixture`。
- 新增 `TechnicalAnalysisAgent`，通过现有 Harness 计算最新单日收益率、SMA5 和 SMA20。
- 输出 `bullish`、`bearish` 或 `mixed`，并附带触发规则、样本数量、数据时间和来源；结果明确不构成投资建议。
- 数据不足 20 根或 `market_data` 类型错误时抛出稳定错误，Harness 会保留失败 trace 和原始原因。
- 新增可直接运行的 `Scripts/demo_technical_analysis.py`，终端可查看结构化指标和 Harness trace。
- 验证结果：新增 5 项 Agent 与演示测试，项目共 35 项 unittest 全部通过；源码、测试和脚本编译检查通过。

下一步
- 把离线行情读取、技术分析和结果汇总接成最小 Graph 工作流，验证金融模块与阶段一平台骨架的组合。

[2026-08-04] 任务书对齐修正
- 复核后确认，先前的“周任务完成”表示内部原型里程碑完成，不能直接代表任务书正式任务完成。
- 建立 `ROADMAP.md`，统一记录正式任务、完成定义、阶段门槛和修正版第 5 至第 19 周计划。
- `checklist.json` 改为 T1.1 至 T4.3 的正式任务口径，同时保留已经完成的 W0 至 W4 内部里程碑证据。
- 当前真实状态：T1.1 完成；T1.2、T1.3、T1.4、T2.1、T2.2 进行中；阶段三和阶段四尚未开始。
- README 和 SPEC 已撤回“阶段一完成”的表述，历史记录不删除，但以后不再用周数替代正式验收。

下一步
- 暂缓最小金融 Graph，先完成 T1.4 的五类 Guardrail，并按总纲通过阶段验收后再推进。

[2026-08-04] 最终交付目标补全
- 重新核对任务书正文、表格和最终 Checklist，把计划从周数驱动改为最终成果和验收证据驱动。
- 补入此前遗漏的非金融 Demo、真实 LLM Model Gateway、Hook 事件循环、Graph 边 Schema 和 LangGraph 映射。
- 明确四类金融 Agent 的指标与输出、2–3 轮辩论协议、风控数值、人工确认和 20 只股票端到端交付。
- 明确夏普基线、可观测指标、Harness 对比实验、1–2 周模拟运行和最终文档清单。
- 保留并强化现有优势：Decimal、source/timestamp/as_of、离线 fixture、失败 trace、Checkpoint、信号/执行时间分离和真实交易关闭。
- `checklist.json` 增加平台、金融应用和工程化三项最终验收结果，避免局部任务完成后误判整个项目已经完成。

下一步
- 当前仍先完成 T1.4 的五类 Guardrail；后续实现顺序可按依赖调整，但最终验收项不得省略。

[2026-08-04] 任务 1.4 Harness SDK 完成
- 实现 JSONSchemaValidator、SourceAttributionFilter、RateLimiter、KeywordBlocker 和 CrossValidator 五类内置 Guardrail。
- 新增 GuardrailRegistry：可以从统一配置创建五类规则，也支持注册自定义 Guardrail 插件。
- Harness trace 现在记录每条规则在输入和输出阶段的开始、通过或失败；配置错误与运行时违规使用不同错误类型。
- JSON Schema 使用明确的确定性子集，不支持的关键字会在配置阶段拒绝；限流器明确为单进程滑动窗口，不冒充分布式限流。
- 新增离线演示，展示五类规则全部通过、误导性关键词在输出阶段被拦截，以及超额调用在输入阶段被限流。
- 验证结果：新增 15 项 Guardrail 和演示测试，项目共 51 项 unittest 全部通过；源码、测试和脚本编译检查通过。

下一步
- 继续完成 T1.2，先实现规划、行动、观察、反思和受控工具调用闭环。

[2026-08-04] 任务 1.2 认知 Loop 与受控工具切片完成
- 新增 Plan、Action、Observation 和 Reflection 稳定契约，Reflection 明确区分继续、修正和完成。
- 新增 ToolRegistry 工具允许列表；未知工具不能执行，每次 Action 的参数和工具结果都经过 Harness 前后校验。
- 工具异常或 Guardrail 拒绝会形成失败 Observation，Agent 可以根据真实失败修正下一步；同一 Action 支持有限重试。
- 新增 CognitiveLoopState、工具执行记录和认知 trace；超过最大步数时安全停止，并保留状态、Harness trace 和原始原因。
- 新增离线计算演示：第一次参数校验失败，Reflection 修正后第二次调用已注册工具并完成。
- 验证结果：新增 7 项认知 Loop 与演示测试，项目共 58 项 unittest 全部通过；源码、测试和脚本编译检查及 checklist JSON 解析通过。
- T1.2 仍为进行中：工作/项目/组织记忆、三类调度、任务隔离、上下文注入、Model Gateway 和非金融 Demo 尚未完成。

下一步
- 为认知 Loop 增加有容量边界、可 JSON 快照恢复的工作记忆，再逐步扩展项目记忆和组织记忆。

[2026-08-05] A2 / 任务 1.3 Graph Engineering 完成
- 新增版本 1 YAML/JSON 工作流加载器和 NodeRegistry；配置只能绑定允许列表中的处理函数，条件使用固定操作符，不执行任意表达式。
- 每条 GraphEdge 强制声明 source 输出 Schema 和 target 输入 Schema，复用 Harness SDK 的确定性 JSON Schema 子集做传递校验。
- GraphRunner 支持顺序或并行拓扑波次；并行节点读取同一状态快照，按声明顺序合并，重复写入同一状态键会明确拒绝。
- 新增节点级有限重试和软超时；新增 closed/open/half_open 熔断状态，并把节点尝试次数和熔断状态写入版本 2 Checkpoint。
- 保留条件分支、跳过传播和故障恢复；并行波次中已成功节点可以进入 Checkpoint，恢复时不重复执行。
- 新增 GraphVisualizer，可输出静态 DAG 或按 pending/completed/skipped/failed 着色的 Mermaid。
- 新增自研 Graph 与 LangGraph 的概念映射，明确当前不引入 LangGraph 运行依赖。
- 新增完整离线演示：从 YAML 加载两个并行分析节点，其中一个节点首次失败后重试，最终输出 Checkpoint、熔断状态和 Mermaid 文件。
- 环境验证：项目使用 D:\Anaconda\python.exe（Python 3.13.2）；PyYAML 6.0.3 已安装到该解释器并写入 pyproject 依赖。
- 验证结果：新增 11 项 Graph Engineering 与演示测试，项目共 69 项 unittest 全部通过；源码、测试和脚本编译检查及 checklist JSON 解析通过。
- T1.3 已达到总纲 A2 的完成口径，checklist 状态更新为 done，remaining 为空。

下一步
- 返回当前主线 T1.2，完成有容量边界、可 JSON 快照恢复的工作记忆最小闭环。

[2026-08-05] 任务 1.2 工作记忆最小闭环完成
- 新增稳定的 MemoryEntry、WorkingMemoryView 和版本 1 WorkingMemorySnapshot 契约，只接受可序列化、有限数值的 JSON 数据。
- 新增容量固定的 WorkingMemory；容量满后按 FIFO 淘汰最旧条目，并持续记录已淘汰数量和单调递增序号。
- CognitiveLoopRunner 会把 Plan、Action、Observation 和 Reflection 摘要写入工作记忆；Agent 在选择动作和反思时读取同一份不可变视图。
- 新增统一 WorkingMemoryStore 接口以及内存、JSON 两种实现；JSON 使用临时文件替换，支持跨运行恢复并拒绝损坏或不兼容的快照。
- 新增离线纠偏演示：第一次参数校验失败，Agent 根据工作记忆修正参数；容量 5 时淘汰最旧两条，随后从 JSON 快照恢复最近条目。
- 保留完整 CognitiveLoopState 历史、最大步数、有限重试、Harness 失败 trace 和安全停止；工作记忆不替代审计历史。
- 验证结果：新增 7 项工作记忆与演示测试，项目共 76 项 unittest 全部通过；源码、测试和脚本编译检查、依赖检查及 checklist JSON 解析通过。
- T1.2 仍为进行中：项目/组织记忆、三类调度、任务隔离、上下文注入、Model Gateway 和非金融 Demo 尚未完成。

下一步
- 为 T1.2 增加按 project_id 隔离、可跨 Loop 运行持久化的项目记忆最小闭环。

[2026-08-05] A3 / 任务 1.2 Loop Engineering 完成
- 新增 LongTermMemory，以项目和组织命名空间隔离长期事实、决定、产物和约定；支持显式 upsert、筛选查询、受控删除、revision、来源和带时区时间。
- 新增内存与 JSON 长期记忆适配器；项目和组织记忆可以跨 Loop 运行恢复，损坏、重复或版本不兼容快照会被拒绝。
- 新增 ContextInjector，把调用方明确选择的 Skill、项目约定、任务上下文、项目记忆和组织记忆组合成只读 InjectedContext；未选择的长期记忆不会自动注入。
- CognitiveLoopRunner 会把同一注入上下文提供给 Planner、Action 和 Reflection，同时继续保留有界工作记忆、最大步数、工具重试、失败 trace 和安全停止。
- 新增 TaskWorkspaceManager；每次调度运行使用独立持久目录，任务 ID、绝对路径和 `..` 路径逃逸会被拒绝；本地目录写入已忽略的 `.runtime/`。
- 新增 HeartbeatLoop 和五字段 CronLoop，按时间槽或 Cron 分钟生成持久幂等键；宿主通过确定性 tick 驱动，不在库内部启动永久后台线程。
- 新增 HookLoop，按订阅名称过滤事件，并用 hook_id + event_id 防止重复处理；事件 payload 作为 JSON 兼容任务上下文注入。
- 新增 GoalLoop，按子目标后序执行和汇总，使用 max_depth、max_goals 硬限制递归规模，并通过 run_id + 目标路径恢复已完成目标。
- 三类触发方式共用 LoopDispatcher 和版本 1 LoopRunLedger；记录成功或失败、执行时间、触发来源和工作目录，JSON 损坏或版本不兼容时拒绝恢复。
- 新增 A3 综合离线演示：Heartbeat、Cron、Hook 和三个递归目标节点共执行 6 次 Cognitive Loop，形成 6 条运行记录和 6 个独立工作目录。
- 验证结果：新增 19 项长期记忆、上下文、工作目录、调度和演示测试，项目共 95 项 unittest 全部通过；源码、测试和脚本编译检查、依赖检查及 checklist JSON 解析通过。
- 任务 1.2 已满足 ROADMAP A3 的完成口径并标记为 done；A4 Model Gateway 和 A5 非金融 Demo 单独保留为平台交付缺口。

下一步
- 进入 A4：先实现确定性 Mock Model Gateway 的稳定请求、响应、错误和追踪契约，再做一种真实 LLM 的最小受控验证。

[2026-08-06] A4 统一 Model Gateway 离线实现完成
- 新增稳定的 ModelRequest、ModelResponse、ModelUsage、ModelRetryPolicy、ModelTraceEvent 和归一化错误契约；成功与失败都会保留模型、token、耗时、尝试次数和有序 trace。
- 新增确定性 MockModelAdapter，支持固定结果和脚本化故障，可离线验证结构化输出、重试、超时与失败路径。
- 新增 OpenAIResponsesAdapter，使用 Responses API，把统一请求转换为 input 和 text.format JSON Schema，并解析实际模型、响应 ID、output_text 与 token 用量。
- Gateway 会对结构化结果进行本地 JSON Schema 二次校验；临时限流、超时、连接故障和 5xx 可有限重试，认证、权限、额度、无效请求、拒答和错误响应立即失败。
- OpenAI 密钥只从 OPENAI_API_KEY 读取；默认演示和全部单元测试不联网、不消耗额度，真实请求必须显式使用 --live。
- A4 定向验证 15 项、项目完整回归 110 项测试全部通过；源码和脚本编译、checklist JSON、Git diff 格式检查通过，离线演示成功输出 provider、model、status、token、latency、attempts、structured_output 和 trace。
- A4 仍为进行中：本机未配置 OPENAI_API_KEY，尚缺一次最小真实 Responses API 调用及实际字段核对，未提前标记为 done。

下一步
- 用户在本地临时设置 OPENAI_API_KEY 后，运行 `python Scripts\demo_model_gateway.py --live --model gpt-5.6-luna`，完成 A4 最后一项真实验证。

[2026-08-06] A4 增加 DeepSeek 真实模型适配器
- 根据 DeepSeek 当前官方接口新增 DeepSeekChatAdapter，使用 Chat Completions、DEEPSEEK_API_KEY 和 deepseek-v4-flash；不再使用已经过弃用日期的 deepseek-chat 默认名。
- DeepSeek JSON Output 只保证合法 JSON，适配器会把目标 Schema 写入 system prompt，返回后继续由统一 Gateway 做本地 JSON Schema 严格复核。
- 将 DeepSeek 的 prompt/completion/total token、实际模型名、响应 ID 和 finish_reason 转换为平台统一契约。
- 将 401、402、429、500/503 分别归一化为认证、额度、限流和服务故障；只有临时故障进入有限重试。
- 抽取模型适配器共用的 JSON HTTP 实现，OpenAI 与 DeepSeek 继续复用同一 ModelGateway interface，Loop、Graph 和 Harness 不需要因供应商变化而修改。
- 新增 DeepSeek 离线适配器测试和无密钥演示测试；所有测试使用假的环境字典和传输层，不访问真实接口。
- 验证结果：A4 相关 21 项、项目完整回归 116 项 unittest 全部通过；源码和脚本编译、checklist JSON、Git diff 格式检查及默认离线演示通过。

下一步
- 用户在本地临时设置 DEEPSEEK_API_KEY 后，运行 `python Scripts\demo_model_gateway.py --live --provider deepseek --model deepseek-v4-flash`，完成 A4 最后一项真实验证。

[2026-08-06] DeepSeek 首次真实调用与语义校验修正
- 用户真实调用成功：provider=deepseek、model=deepseek-v4-flash、attempts=1、input/output/total token=123/13/136、latency=974ms，证明网络、认证、模型和用量字段映射均可用。
- 返回的结构化结果为 mode=offline，与真实运行事实不一致；原 Schema 同时允许 offline 和 live，因此格式校验通过但语义校验没有拦截。
- 新增红色回归测试，确认 live Schema 原本允许 offline；随后改为按运行模式动态生成 Schema，live 只接受 live，offline 只接受 offline，并把提示词改为明确固定值。
- 演示新增 response_id 输出，便于核对真实供应商响应；A4 暂不提前完成，等待修正后的最小真实请求复验。
- 修正后项目完整回归 118 项 unittest 全部通过，源码和脚本编译、checklist JSON 与 Git diff 格式检查通过。

下一步
- 在同一个已设置 DEEPSEEK_API_KEY 的 PowerShell 中重新运行真实演示；输出必须包含 mode=live 和非空 response_id。

[2026-08-06] A4 统一 Model Gateway 正式完成
- 修正后的 DeepSeek 真实调用成功：provider=deepseek、model=deepseek-v4-flash、response_id=b78413b7-fb38-4621-ac51-4ba3eb0925e2、status=succeeded、attempts=1。
- 实际 input/output/total token 为 116/12/128，耗时 1063ms；结构化输出 mode=live，通过收紧后的本地 Schema 校验。
- A4 已满足统一 Mock/真实模型接口、结构化输出、超时、有限重试、错误归一化、调用追踪、环境变量密钥边界、离线测试和最小真实验证全部条件，checklist 标记为 done。
- 验收证据不保存 API Key 或原始响应缓存，只记录非敏感运行字段；真实交易边界没有变化。

下一步
- 进入 A5 非金融通用性 Demo，复用现有 Harness、Loop、Graph、Model Gateway、记忆和受控工具接口证明平台可跨领域接入。

[2026-08-06] A5 非金融通用性证明完成
- 新增 `agent_platform.research` 独立领域模块，包含本地资料契约、确定性检索工具、Model Gateway 认知 Agent、证据综合 Agent 和工作流装配；A5 没有修改 core 框架语义。
- 认知 Loop 使用 Plan、Action、Observation、Reflection 和有界工作记忆，只能通过 ToolRegistry 调用 `local_document_search`；工具结果经过 JSON Schema 和 source/timestamp/as_of 校验。
- 新增三节点 YAML Graph：retrieve 负责 Loop 检索，organize 用确定性代码生成证据编号，synthesize 通过 AgentHarness 生成报告；两条边均声明输入输出 Schema。
- 报告 Harness 同时检查结构、来源、主题一致性和证据编号；不存在的 evidence_id 会被 CrossValidator 拒绝，模型不能绕过确定性来源约束。
- 默认演示使用四次脚本化 Mock 调用，不联网、不消耗额度；保留显式 `--live` DeepSeek 模式，真实调用继续只读取当前终端的 DEEPSEEK_API_KEY。
- 新增故障恢复演示：synthesize 首次失败后从版本 2 Checkpoint 恢复，恢复阶段节点调用为 retrieve=0、organize=0、synthesize=1，已完成节点没有重复执行。
- A5 定向验证 10 项、项目完整回归 128 项测试全部通过；源码和脚本编译、checklist JSON、YAML 工作流解析、Git diff 格式和运行文件忽略规则检查通过。
- 离线恢复演示成功输出三节点完成状态、1 步认知 Loop、1 条带来源证据、4 次模型调用、128 个模拟 token 和完整 Graph trace。
- 新领域接入在 2026-08-06 的单次开发阶段内完成，低于两天验收上限；接入步骤和限制记录在 `docs/non-financial-research-demo.md`。
- A5 和 PLATFORM 已在 checklist 标记为 done；这只代表通用平台交付包完成，不代表金融应用和工程化任务已经完成。

下一步
- 进入 B1 金融数据 MCP，先做一个真实金融数据接口的最小字段、时间、权限和错误验证，再定义统一工具契约与离线回放。

[2026-08-06] 交付包一严格验收缺口修复完成
- 复核发现 checklist 曾声明存在 dev-map，但仓库实际缺失；Skill、MCP、SubAgents 也只有占位说明，Echo 只有单元测试而没有独立运行入口，因此此前 PLATFORM=done 的判断过早。
- 新增根目录 `dev-map.md`，逐项连接 SPEC、Rule、Skill、Workflow、Scripts、MCP、SubAgent、dev-map 和任务看板，并记录核心 module 的稳定 interface、adapter 和验证入口。
- 为 Skill、MCP 和 SubAgents 新增版本 1 JSON catalog；active 条目记录 interface、implementation、import_path 和 evidence，pending 金融市场数据 MCP 明确不冒充完成。
- 项目引导测试现在会导入每个 active 条目的真实实现符号，并核对 implementation 和全部 evidence 文件存在，管理证据不再只靠文字声明。
- 新增 `Scripts/demo_echo.py` 和 subprocess 测试；演示通过 AgentHarness 原样返回任务，并输出 preflight、agent 和 postflight 完整 trace。
- 更新三个目录的角色说明、根 README、docs 索引、SPEC、ROADMAP 和 checklist；PLATFORM=done 现在有可核查证据支撑。
- 验证结果：新增 3 项自动化检查后，项目完整回归 131 项全部通过；Echo 独立演示、源码/脚本编译、四份 JSON 解析全部成功。
- 本次只修复交付包一证据，没有实现 B1 真实金融数据 MCP，也没有修改 Harness、Loop、Graph、Memory 或 Model Gateway 的核心 interface。

下一步
- 进入 B1 金融数据 MCP，先运行一个最小真实数据请求并检查返回字段、时区、权限和错误，再冻结 adapter interface。
