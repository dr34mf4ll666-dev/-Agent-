# Week 0 defaults: offline and safe by default.
LLM_PROVIDER=mock
DATA_PROVIDER=fixture
ALLOW_LIVE_TRADING=false

# Fill these only in a local, uncommitted .env file when a later stage needs them.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-5.6-luna
# DEEPSEEK_API_KEY=
# DEEPSEEK_MODEL=deepseek-v4-flash
# TUSHARE_TOKEN=
